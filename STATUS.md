# 📍 STATUS.md — Pooilgroup ERP

> **Source of truth สำหรับสถานะจริง** — อัพเดต 2026-09-23 (🪑✨✅ ChairOps PDF สัญญา — รูปไม่ขึ้น (สัญญาเก่าก่อนมีฟีเจอร์เซลฟี่ ไม่มีรูปติดสัญญาจริง) + จัดหน้าหนักซ้ายไม่สมส่วน + ขอข้อมูลตัวหนา — แก้ครบ: เพิ่ม fallback ใช้รูปโปรไฟล์ปัจจุบันถ้าสัญญาเก่าไม่มีรูป, จัดหัวกระดาษใหม่ให้ชื่อเรื่องอยู่กลางจริง กล่องรูปลอยมุมขวาไม่แย่งพื้นที่, ข้อมูลที่กรอกเป็นตัวหนาเส้นประ — **DEPLOYED LIVE** `3ca906c2` ทดสอบด้วย login จริง+ดาวน์โหลดจริงแล้ว · 🪑📄✅ ChairOps หน้าสัญญาฝั่งออฟฟิศ — ปุ่ม PDF ยังเป็น window.print() เดิม (ประทับ URL+เวลาเบราว์เซอร์ลงหน้ากระดาษ ไม่สวย) + สัญญาขาดบรรทัด "ทำ ณ วันที่..." เทียบกับต้นฉบับที่ CEO ส่งมา — แก้ทั้งคู่ ใช้ PDF จริงแบบเดียวกับฝั่งแม่บ้าน + เพิ่มบรรทัดวันที่ครบ **DEPLOYED LIVE** `fc2c8560`, verify ด้วย login จริง + เทียบภาพกับต้นฉบับ · 🪑🖼️✅ ChairOps หน้าโปรไฟล์แม่บ้าน (ออฟฟิศ) — รูปเซลฟี่/บัตรประชาชนกดดูรูปเต็มไม่ได้เลย (บัตร ปชช. เดิมมีแค่คำว่า "แนบแล้ว" ไม่มีรูปให้ดูด้วยซ้ำ) — เพิ่มลิงก์คลิกดูรูปเต็มทั้งคู่ **DEPLOYED LIVE** `82962209`, verify ด้วย login จริง · 🪑📄✅ ChairOps เซ็นสัญญา — ปุ่ม "พิมพ์/บันทึก PDF" กดไม่ออกใน LINE in-app browser (window.print ใช้ไม่ได้) เปลี่ยนเป็นดาวน์โหลด PDF จริง (server-side @react-pdf/renderer) + เพิ่มรูปเซลฟี่มุมขวาบนสัญญา — **DEPLOYED LIVE** `380d7012`, ทดสอบด้วยข้อมูลจริงแล้ว (เห็น log ยืนยัน user-agent มี "Line/26.15.0" จริง) · 🪑📸✅ ChairOps onboarding — นายหน้าถ่ายรูปบัตร/เซลฟี่แม่บ้านไม่ได้เลย 3 บั๊กซ้อนกัน (ปุ่มถ่ายเลยกดไม่ติด, เลือกรูปจากเครื่องถูกบังคับเป็นกล้องอย่างเดียว, อัปโหลดพังด้วย CSP บล็อกทุกครั้งไม่ใช่แค่เน็ตหลุด) — **DEPLOYED LIVE** `92a2c3ca`, รอนายหน้าลองซ้ำจริงเพราะ headless ยืนยันได้แค่ level โค้ด · 🏨🔗✅ CashHub ปุ่ม "ตรวจยอดขายโรงแรม" เด้งไปหน้าข้อมูลเก่าเดือนเมษายน 2569 แก้แล้ว — **DEPLOYED LIVE** `559864cf` · 🪑🎯🚀 ChairOps audit→bigsolvebug→upspeed→CEO-decisions ครบวงจรวันเดียว — 11 บั๊กแก้ + FIN-03 กันเงินรอตรวจ + เชื่อมสูตรปิดงวด + audit-export ใช้งานได้ — **DEPLOYED LIVE** `5bce3769` · 👥🔧 LINE login ตอนนี้บันทึกเวลาเข้าใช้แล้ว (แก้รากเสร็จสมบูรณ์ ครบทั้ง 2 ระดับ) — **DEPLOYED LIVE** `08b0fc4b` · 🏬🔴🚀 RentSpace matrix — เห็นกรอบแดงในตารางทันทีถ้ายอดสลิปไม่ตรง (ไม่ต้องกดปุ่มส่งก่อน) — **DEPLOYED LIVE** `e99805df` · 🏬🚀 RentSpace matrix — ปุ่ม "ส่งเข้าบัญชี LedgerLine" ย้ายมาไว้หน้าตารางค่าเช่า + ด่านเช็คยอดสลิปก่อนส่ง (เจอ 4 บิลจริงยอดไม่ตรง กันไว้ไม่ให้ส่ง) — **DEPLOYED LIVE** `c910c553` · 🧾🔧🚀 LedgerLine VAT อ่านผิดเป็น 0 บนบิลราคาต่อชิ้นรวม VAT — **DEPLOYED** `7de3ec1e`, backfill ใบ Dohome แล้ว, ไล่เช็ค 8 ใบทั้งระบบพบอีก 1 ใบโดนเหมือนกัน (AP 551563 — ผูกกับปัญหา GL/PV เดิมที่ค้างอยู่) รอ CEO ตัดสินใจ · 🪑📤✅ ChairOps เลือกหลายสาขาส่งเข้า reconcile ทีเดียว (จาก Pinpoint) — **DEPLOYED LIVE** `4aa39276` · 🧾⚡ LedgerLine รายจ่าย คลิกเปลี่ยนบิลรู้สึกเหมือน refresh — **DEPLOYED LIVE** `6d2b33c3`, smoke ยืนยันแล้ว · 🏬🧾 RentSpace export รายงานสรุปค่าเช่าจากหน้า matrix — **DEPLOYED LIVE** `61fb3638`, smoke ยืนยันแล้ว · 🦞🧾 ClawFleet แนบสลิปฝากเงิน+AI อ่านยอด จากหน้าประวัติเก็บเงิน — **DEPLOYED LIVE** `80b37319` · 🧾 RentSpace คลิกดูสลิป+AI ตรวจสลิปต่อรายการชำระ — **DEPLOYED LIVE** `c3ac784f`, รอ CEO ตั้งค่าบัญชีธนาคารก่อนใช้จริง · ✅ ChairOps "ควรได้"(มิเตอร์) บั๊ก zero-fallback org-wide — DEPLOYED LIVE `edbe8832`)

## 🏬🔌✅ RentSpace — LINE OA ผูกบัญชีไม่ติด (rich-menu ใช้ผิดช่องทาง) + ออกบิลหลายห้องพร้อมกัน (2026-09-25)

CEO ส่งสกรีนช็อตพร้อมกัน 4 เรื่อง — 2 เรื่องแรกลงมือทำทันที (ไม่มีคำถามค้าง), 2 เรื่องหลัง (สี/จัดลำดับมิเตอร์ + ใบกำกับภาษี) รอคิวถัดไป

**1) 🔴 บั๊ก LINE OA "ยังไม่ได้เชื่อมบัญชี" ทั้งที่เชื่อมแล้ว — กระทบผู้เช่าทุกคนที่กดปุ่มเมนูใน LINE OA มาตั้งแต่สร้างฟีเจอร์ 25 ก.ค. (ไม่ใช่แค่รายนี้):**
สาเหตุ: [`lib/line/channels.ts`](lib/line/channels.ts) ไม่เคยรู้จัก path `/liff/rentspace` เป็นโมดูลของตัวเอง → ตกไปใช้ช่องทาง LINE เริ่มต้น (ของพนักงาน) แทน → [`app/liff/liff-bootstrap.tsx`](app/liff/liff-bootstrap.tsx) เลยยิง `liff.init()` ด้วยช่องทางผิด แข่งกับ `LiffRentspaceClient` ที่ยิงด้วยช่องทางถูก บน SDK ตัวเดียวกัน — id_token ที่ได้เลยตรวจสอบไม่ผ่าน ทั้งที่ฐานข้อมูลผูกไว้ถูกต้องแล้ว
**แก้:** เพิ่ม `"rentspace"` เป็นโมดูลที่รู้จักใน `channels.ts` (ให้ใช้ `NEXT_PUBLIC_RENTSPACE_LIFF_ID` ของตัวเอง — env มีอยู่แล้วใน Vercel) + เพิ่ม early-return guard ใน `liff-bootstrap.tsx` กันไม่ให้ยิงช่องทางพนักงานทับ (ใช้ pattern เดียวกับ guard ที่ ClawHub มีอยู่แล้ว) — เป็นการเพิ่มเข้าไปเฉยๆ ไม่แตะโค้ดของโมดูลอื่น (ChairOps/LedgerLine/ClawHub)
**ตรวจสอบแล้ว:** `next build` เต็มรูปแบบผ่านสะอาด ทุก route รวม `/liff/rentspace` `/liff/ledger` `/liff/clawhub` ขึ้นครบไม่มี error · eslint สะอาด · ⚠️ **ยังไม่มีทางทดสอบ flow จริงในเบราว์เซอร์ LINE ได้จากแซนด์บ็อกซ์นี้** — ยืนยันได้แค่ระดับโค้ด+build เท่านั้น ต้องรอผู้เช่าจริงลองกดเมนูใน LINE OA อีกครั้งหลัง deploy

**2) ✅ ออกบิลหลายห้องพร้อมกัน — ของมีอยู่แล้ว แค่ยังไม่ได้ต่อสายมาที่หน้าออกบิลหลัก:**
หน้า "จดมิเตอร์" มีระบบติ๊กเลือกหลายห้อง+พรีวิวยอด+ยืนยันออกบิลอยู่แล้ว ([`SelectiveBillPanel`](<app/(admin)/rentspace/meters/_components/selective-bill-panel.tsx>)) และฝั่งเซิร์ฟเวอร์กันบิลซ้ำระดับฐานข้อมูลอยู่แล้ว (unique constraint) — แค่เพิ่มปุ่ม "เลือกหลายห้อง" ที่หน้า "ใบแจ้งหนี้/บิล" หลัก เปิด modal เอาคอมโพเนนต์เดิมมาใช้ซ้ำ (ไม่สร้างใหม่) เลือกได้กี่ห้องก็ได้ ไม่บังคับทั้งหมดหรือทีละใบเหมือนเดิม
**แก้ที่:** [`app/(admin)/rentspace/bills/page.tsx`](<app/(admin)/rentspace/bills/page.tsx>) (ดึงข้อมูลห้อง+สถานะมิเตอร์+สถานะออกบิลของงวดนี้) · [`bills-actions.tsx`](<app/(admin)/rentspace/bills/_components/bills-actions.tsx>) (ปุ่ม+modal ใหม่) · `selective-bill-panel.tsx` (เพิ่ม callback ปิด modal อัตโนมัติหลังออกบิลสำเร็จ — ไม่กระทบหน้าจดมิเตอร์เดิม)
**ตรวจสอบแล้ว:** `next build` ผ่านสะอาด · eslint ไฟล์ที่แก้ 0 error (มี 1 error เดิมที่ไม่ได้แตะในไฟล์ `selective-bill-panel.tsx` — หนี้ lint เก่าของโปรแกรม ไม่เกี่ยวกับงานนี้)

**Housekeeping ระหว่างทาง:** STATUS.md เกินงบ RULE M (207KB) → archive ชุด 2026-08-28→09-09 ไป STATUS-archive.md (→141KB)

## 📄🏛️✅✅ DocuFlow — เมนูเยอะเกินไป งง + ปรับ design + Google Drive + ลายเซ็นใหม่ (2026-09-23)

CEO ส่งสกรีนช็อต 4 รูปบ่นว่าเมนู DocuFlow ซ้ำซ้อนจนงง อยากจัดใหม่ + ปรับหน้าตาให้เหมือน ChairOps/ClawFleet/RentSpace/CashHub — สั่งผ่าน `/feature workshop` รันทีม 13 คนเต็มรูปแบบ → ล็อก spec → CEO ตอบ 4 คำถามหลัก (Google Drive เชื่อมเลย ใช้ของเดิม / LINE OA แยกทีหลัง / ลายเซ็นทำให้จบเต็มรูปแบบ / จัดเมนูก่อน) → สั่ง `/goal` ให้ทำให้จบทั้งหมด → เขียนโค้ดเสร็จครบทั้ง 2 กลุ่มใน git worktree แยก (`docuflow-redesign`) **commit แล้ว 2 commit ยังไม่ push/deploy** รอ CEO ตรวจก่อน:

**กลุ่ม A** (commit `1a7aae21`): เปิดหน้า "ตั้งค่า" ใหม่ทั้งหมด (โมดูลเดียวที่เคยไม่มี) + รวมหน้าเอกสารเหลือหน้าเดียว filter ใช้งานจริง (เดิมกดไม่ได้) + สร้าง "ประเภทเอกสาร" เป็นของจริงในฐานข้อมูล (เดิม hardcode) + ฟอร์มอัปโหลดเลือกบริษัท/สาขา/ประเภท+แนบหลายไฟล์ + ลบเลข "142 ครั้ง" ปลอมที่ไม่มีคนใช้จริง + แก้บั๊กฟอนต์ไทยในลายเซ็น (เดิมขึ้น "?") + เมนูใหม่ 5 ปุ่ม (หน้าหลัก/เอกสาร/อัปโหลด/ปฏิทิน/ตั้งค่า) + ปรับสีทั้งโมดูลให้เหมือน ChairOps/CashHub

**กลุ่ม B** (commit `56ab11a8`): เชื่อม Google Drive จริง (ใช้ connection เดิมของ ChairOps ที่มีอยู่แล้ว ไม่ต้องต่อใหม่ ไม่เสียเงินเพิ่ม) ปุ่ม "ส่งออกไป Drive" ต่อเอกสาร + หน้า "ลายเซ็นของฉัน" ใหม่ที่ `/profile/signature` ให้บันทึกลายเซ็นไว้ใช้ซ้ำ + หน้าเซ็นเอกสารใหม่ให้เห็นเอกสารเต็มฉบับ+ตำแหน่งลายเซ็นทุกจุดก่อนกดยืนยัน (เดิมเห็นแค่หน้าที่มีช่องเซ็น) + เพิ่มปุ่มล็อกอินด้วย LINE ที่หน้า login (เดิมเข้าทางนี้ไม่ได้เลยนอกแอป LINE)

**ตรวจสอบแล้ว:** `tsc --noEmit` / `next build` (ของจริง ไม่ใช่แค่ compile) / `eslint` ผ่านสะอาดทั้ง 2 รอบ · migration ฐานข้อมูลยืนยันขึ้นจริงแล้วผ่าน `scripts/check-schema-applied.mjs` (ตัวเช็คที่กันเหตุการณ์ ChairOps ล่มทั้งโปรแกรมเมื่อ 19 มิ.ย.) · ถ่ายรูปหน้าจอในเครื่องไม่ได้เพราะ `next dev`/Turbopack โหลดฟอนต์ Google ไม่ได้ในแซนด์บ็อกซ์นี้ (ปัญหาเดิมที่เจอมาก่อน ไม่เกี่ยวกับโค้ดที่เขียนรอบนี้ — `next build` จริงผ่านปกติ 5 รอบ) — รอดู UI จริงตอน deploy

**ยังไม่ทำ (CEO สั่งแยกทำทีหลัง):** LINE OA แจ้งเตือนเอกสารใกล้หมดอายุ

**เอกสารเต็ม:** [`docs/WORKSHOP_docuflow-redesign.md`](docs/WORKSHOP_docuflow-redesign.md) (สเปก+มติ CEO) · [`docs/BIGFEATURE_docuflow-redesign_TRACKA_PLAN.md`](docs/BIGFEATURE_docuflow-redesign_TRACKA_PLAN.md) · [`docs/BIGFEATURE_docuflow-redesign_TRACKB_PLAN.md`](docs/BIGFEATURE_docuflow-redesign_TRACKB_PLAN.md)

**สถานะล่าสุด:** ✅ **DEPLOYED LIVE** — CEO สั่ง "Push deploy เลย" → รัน `/verify` (tsc+eslint+build+git status+smoke test) ผ่านทุกจุด → push เข้า `setup` (`e6331716`, fast-forward สะอาด ไม่มีคนอื่นแก้ชนกัน) → Vercel deploy อัตโนมัติ → ยืนยันด้วย headless screenshot login จริงบน pooilgroup.com เห็นหน้าตาใหม่ทำงานได้จริง (ส่งรูปให้ CEO แล้ว)

**รอบ 2 (2026-09-24):** CEO ดูของจริงแล้วพบว่าเมนู "ฝั่งซ้าย" (desktop sidebar) ที่ใช้งานจริงยังไม่ได้แก้ (รอบแรกแก้แค่เมนูมือถือ) — แก้ 15→6 รายการจริง (commit `a62cac6e`, **DEPLOYED LIVE**) + ลบเอกสารทดสอบ 41 ใบที่ไม่ใช่ของจริงออกหมด (soft-delete, CEO ยืนยัน 2 รอบ) · ตามด้วย feedback ก้อนใหญ่รอบ 2: กลุ่มเอกสาร (taxonomy ชั้นที่ 2 แยกอิสระจากประเภทเอกสาร), แก้ dropdown ประเภทเอกสารไม่ให้โชว์รายการมาตรฐานทั้งหมดอัตโนมัติ (ต้องเป็นของที่ตั้งค่าไว้จริงเท่านั้น), วันที่ออกเอกสาร, ระบบ tag ค้นหา, โหมด "เอกสารเดียวหลายไฟล์" (รวมเป็น PDF อัตโนมัติ), เชื่อมอัปโหลด→หน้าตั้งตำแหน่งลายเซ็นที่มีอยู่แล้ว, ผูกลายเซ็นแทนพนักงานได้จากหน้าแก้ไขผู้ใช้ (commit `339b5ad5`)

**⚠️ เหตุการณ์ deploy พลาดรอบหนึ่ง:** push `339b5ad5` ครั้งแรก Vercel build **ล้มเหลวจริง** (`● Error` ใน `vercel ls`) ด้วย error เดียวกับที่เจอตอน `next dev` ในแซนด์บ็อกซ์ (`next/font/google` โหลดฟอนต์ไม่ได้) — ต่างจากที่คิดไว้ก่อนหน้าว่าเป็นปัญหาเฉพาะ dev-mode ในแซนด์บ็อกซ์เท่านั้น คราวนี้เกิดขึ้นจริงบน Vercel build ด้วย (โค้ดเดิมเป๊ะ ก่อนหน้าผ่านมาแล้วหลายรอบ — น่าจะเป็น Google Fonts fetch หลุดชั่วคราวตอน build ไม่ใช่บั๊กจากโค้ดที่เขียน) แก้ด้วย `vercel redeploy <url>` ครั้งเดียวผ่านเลย ยืนยันแล้วว่าขึ้นจริง (`● Ready`) — **บทเรียนสำหรับรอบหน้า: หลัง push ต้องเช็ค `vercel ls` ว่า build จริงผ่านไหม ไม่ใช่แค่ดูว่าเว็บไซต์ยังตอบ 200/307 อยู่ (เพราะเว็บจะยังโชว์ deploy เก่าที่ดีอยู่ต่อไปเรื่อยๆ แม้ deploy ใหม่จะพังก็ตาม — เคยเข้าใจผิดว่า route เดิมตอบ 307 = deploy ใหม่สำเร็จ ทั้งที่จริงคือ route เก่าที่ยังไม่เปลี่ยนตอบเหมือนเดิม)**

**รอบ 3 / Track D (2026-09-24):** CEO ขอ 3 เรื่องเพิ่มบนหน้าอัปโหลด+รายการเอกสาร (commit `a11b19da`, **DEPLOYED LIVE**):
1. ปุ่ม "+ สร้างใหม่" ข้างดรอปดาวน์ "ประเภทเอกสาร" ในฟอร์มอัปโหลด — สร้างประเภทเอกสารใหม่ได้ทันทีในป็อปอัพเล็กๆ (ชื่อ + ประเภทธุรกิจ + **บริษัท**) โดยไม่ต้องออกจากหน้า — เดิมต้องกดลิงก์ไปหน้าตั้งค่าแยกต่างหาก ข้อมูลที่กรอกในฟอร์มจะหาย
2. เพิ่มคอลัมน์ `document_types.company_id` (nullable, FK → companies) — CEO เลือก "ทั้งสองอย่าง" ตอนถามว่า "เลือกบริษัท" หมายถึงประเภทธุรกิจ (ปั๊ม/โรงแรม/...) หรือนิติบุคคลจริง (Pooil Oil/JP Sync Group) — เพิ่มทั้งคู่ ประเภทธุรกิจมีอยู่แล้ว เพิ่มใหม่แค่บริษัท — migration additive/nullable, apply prod จริงแล้ว verify 0 drift ผ่าน `check-schema-applied.mjs` ก่อน push (ตาราง `document_types` มี 0 แถวตอนเริ่มทำ ไม่มีข้อมูลเก่าต้องย้าย)
3. ตัวเลือก "หลายเอกสาร/เอกสารเดียว" ตัดคำอธิบายยาวออก เหลือ label สั้น + จุดติ๊ก
4. หน้ารายการเอกสาร — ตัวกรองเปลี่ยนจากปุ่ม chip (กินที่เยอะ) เป็น dropdown กะทัดรัด + เพิ่ม dropdown "บริษัท" ใหม่ที่กรองได้จริง (query เดิมรองรับ `companyId` อยู่แล้ว แค่ไม่เคยมีช่องให้เลือกตรงๆ มาก่อน — ไม่ใช่บั๊ก เป็นของที่ยังไม่เคยทำ)

ตรวจสอบแล้ว: `tsc`/`eslint`/`next build` สะอาด (0 error ในไฟล์ที่แก้) · migration verify 0 drift · push ผ่าน `/verify` gate (stamp ผูกกับ commit) · Vercel build ครั้งนี้ผ่านตั้งแต่รอบแรก (`● Ready` ใน 3 นาที ตามบทเรียนรอบก่อน) · ยืนยันด้วย headless screenshot login จริงบน pooilgroup.com เห็นปุ่ม "+ สร้างใหม่" และตัวกรอง dropdown ใหม่ทำงานจริง (ส่งรูปให้ CEO แล้ว)

**⚠️ CEO ถามตรงๆ ว่า "ใช้งานได้จริงเชื่อมโยงกันจริงไหม" (2026-09-25) — ทดสอบซ้ำแบบคลิกจริงแล้วเจอ 2 บั๊กจริงที่ tsc/eslint/build ตรวจไม่เจอ:**
1. **ปุ่ม "+ สร้างใหม่" กดไม่ทำงานจริง** — ป็อปอัพสร้างประเภทเอกสารดันมี `<form>` ซ้อนอยู่ใน `<form>` ของฟอร์มอัปโหลดทั้งหน้า (HTML ไม่ยอมให้ฟอร์มซ้อนกัน) กดปุ่ม submit แล้วหลุดไปเข้าฟอร์มนอกแทน ไม่สร้างอะไรเลย — แก้เป็น `<div>` + ผูกปุ่มด้วย onClick ตรงๆ (commit `d230bc52`)
2. **เลือก "บริษัท" แล้ว submit ไม่ผ่านเลยสักครั้ง** — validation ฝั่ง server (`z.string().uuid()`) เข้มงวดเกินจริง บริษัทจริงทั้ง 2 บริษัทในระบบ (Pooil Oil/JP Sync Group) ใช้ id แบบ seed (`00000000-...-0000000000a1/a2`) ที่ไม่ผ่านมาตรฐาน UUID เป๊ะ — เท่ากับฟีเจอร์ "เลือกบริษัท" พังกับบริษัทจริงทุกบริษัท 100% ไม่ใช่แค่จุดทดสอบ — แก้โดยเปลี่ยนไปใช้ `zUUID()` (helper ที่มีอยู่แล้วในระบบ ใช้แก้ปัญหานี้ที่จุดอื่นมาก่อน) แทน `.uuid()` เดิม (commit `fcbc94de`)

**ทดสอบซ้ำแบบครบวงจรจริงหลังแก้ (ไม่ใช่แค่เช็คโค้ด):** คลิกสร้างประเภทเอกสารจริงบนเว็บจริง (ชื่อ+ประเภทธุรกิจ+บริษัท) → เช็ค API ว่าบันทึกค่าครบถูกต้อง → เช็คตรงกับฐานข้อมูลจริงโดยตรง (query แยกอิสระ ไม่พึ่ง API) → เปิดหน้าตั้งค่าเห็นแถวใหม่ขึ้นถูกต้องพร้อมชื่อบริษัท → เปิดหน้ารายการเอกสารเห็นตัวเลือกใหม่โผล่ในตัวกรอง → เลือกตัวกรอง "บริษัท" แล้ว URL เปลี่ยนถูกต้องจริง → ลบทดสอบออกผ่าน API เดียวกับที่ปุ่ม "ปิดใช้งาน" ใช้ ยืนยันสถานะปิดใช้งานถูกต้องทั้งใน API และฐานข้อมูลจริง — **ผ่านครบทุกจุดจริง ไม่มีจุดไหนพังแล้ว** — post-mortem เต็มที่ [`postmortems/docuflow-quickcreate-nested-form-and-uuid-validation-2026-09-25.md`](postmortems/docuflow-quickcreate-nested-form-and-uuid-validation-2026-09-25.md)

---

## 🪑✨✅ ChairOps — PDF สัญญา: รูปไม่ขึ้น + จัดหน้าไม่สวย + ขอตัวหนา (2026-09-23)

CEO ทดสอบ PDF ที่เพิ่งแก้ (ด้านล่าง) ต่อทันที เจอ 3 เรื่อง: (1) กล่องรูปเซลฟี่ว่างเปล่า (2) หน้าตาสัญญา "หนักฝั่งซ้ายไป ดูไม่สมส่วน" (3) ขอให้ข้อมูลที่กรอกเป็นตัวหนา + มีจุดไข่ปลาเหมือนกรอกฟอร์มจริง

**สาเหตุข้อ 1:** ไม่ใช่บั๊ก — สัญญาที่ทดสอบเป็นสัญญาเก่าที่เซ็นไว้**ก่อน**ฟีเจอร์เก็บรูปเซลฟี่ในสัญญาจะมีอยู่ (สร้างเช้านี้) เลยไม่มีรูปติดอยู่ในสัญญาจริงๆ (ไม่ใช่แค่สัญญานี้ — **สัญญาที่เซ็นไปแล้วทุกใบก่อนหน้านี้จะเจอปัญหาเดียวกัน**) แก้โดยเพิ่ม fallback: ถ้าสัญญาไม่มีรูปติดอยู่ ให้ใช้รูปโปรไฟล์ปัจจุบันของแม่บ้านแทน

**สาเหตุข้อ 2:** กล่องรูปกับชื่อเรื่องอยู่แถวเดียวกันแบบ 3 ช่อง (ซ้ายว่าง-กลางชื่อเรื่อง-ขวากล่องรูป) พอฝั่งขวามีกรอบชัดเจนแต่ฝั่งซ้ายว่างเปล่า สายตาเลยรู้สึกไม่สมดุลทั้งที่จริงๆ อยู่กลางพอดี แก้โดยให้ชื่อเรื่องอยู่กลางเต็มความกว้างหน้ากระดาษจริง (เหมือนต้นฉบับเป๊ะ) แล้วให้กล่องรูปลอยอยู่มุมขวาบนแยกต่างหาก ไม่แย่งพื้นที่กับชื่อเรื่อง

**สาเหตุข้อ 3:** ข้อมูลที่กรอกเดิมใช้แค่ตัวธรรมดาขีดเส้นใต้ ไม่ต่างจากตัวหนังสือทั่วไปมากพอ แก้เป็นตัวหนา + เส้นใต้แบบจุด (เหมือนลายมือกรอกทับจุดไข่ปลาบนฟอร์มกระดาษจริง)

**ทดสอบแล้ว:** login จริง ดาวน์โหลด PDF จริง เทียบภาพทีละหน้า — รูปขึ้นแล้ว (ใช้รูปโปรไฟล์ปัจจุบันแทน), หัวกระดาษสมดุลแล้ว, ข้อมูลเป็นตัวหนาชัดเจนครบทุกจุด — commit `3ca906c2` **DEPLOYED LIVE**

---

## 🪑📄✅ ChairOps — สัญญาฝั่งออฟฟิศ ปุ่ม PDF ยังเป็นของเดิม + ขาดบรรทัดวันที่ (2026-09-23)

CEO ส่งสกรีนช็อตผลลัพธ์ตอนกด "พิมพ์ / บันทึก PDF" ในหน้าสัญญาฝั่งออฟฟิศ (`/chairops/maids/[id]/contract`) พร้อมไฟล์สัญญาต้นฉบับ (กระดาษ) มาเทียบ — พบว่าหน้านี้**ยังไม่ได้แก้** (แก้ไปแล้วเฉพาะฝั่งแม่บ้านตอนเช้า) ยังใช้ window.print() เดิม ซึ่งเบราว์เซอร์ประทับ URL+วันเวลาลงหน้ากระดาษเอง ดูไม่เป็นมืออาชีพ + เทียบกับต้นฉบับแล้วพบว่าขาดบรรทัด "ทำ ณ วันที่ ... เดือน ... พ.ศ. ..." ที่ควรมี

**แก้แล้ว:**
1. เปลี่ยนปุ่มฝั่งออฟฟิศเป็นดาวน์โหลด PDF จริงเหมือนฝั่งแม่บ้าน (เพิ่ม API endpoint ใหม่สำหรับออฟฟิศดูสัญญาแม่บ้านคนไหนก็ได้ — ใช้ตัวสร้าง PDF ตัวเดียวกัน ไม่ต้องเขียนใหม่)
2. เพิ่มบรรทัด "ทำ ณ วันที่..." ทั้งในหน้าพรีวิวบนจอและใน PDF ให้ตรงกับต้นฉบับ (ใส่วันที่เซ็นจริงให้อัตโนมัติเมื่อเซ็นแล้ว)

**ทดสอบแล้ว:** login จริงเข้าหน้าออฟฟิศ กดดาวน์โหลด ได้ไฟล์ PDF จริง เทียบภาพกับต้นฉบับที่ CEO ส่งมาแล้วตรงกัน (โครงสร้าง/บรรทัดวันที่/กล่องลงนาม) — commit `fc2c8560` **DEPLOYED LIVE**

**deploy เดียวกันนี้ยังพ่วงแก้อีกจุด:** ปุ่มดูรูปบัตรประชาชนใน "ฟอร์มแก้ไขข้อมูลสัญญา" ฝั่งออฟฟิศ (คนละจุดกับที่แก้ตอนเช้า — จุดเช้าคือหน้าดูข้อมูลอย่างเดียว จุดนี้คือฟอร์มแก้ไข ใช้ component คนละตัว) ก็กดดูรูปเต็มไม่ได้เหมือนกัน เพิ่มลิงก์คลิกดูให้แล้วในคราวเดียวกัน (แก้ที่ component กลางตัวเดียว ครอบคลุมทั้ง 4 จุดที่ใช้ร่วมกัน)

เอกสารเต็ม: [`postmortems/chairops-office-contract-pdf-and-dateline-2026-09-23.md`](postmortems/chairops-office-contract-pdf-and-dateline-2026-09-23.md)

---

## 🪑🖼️✅ ChairOps — หน้าโปรไฟล์แม่บ้าน (ออฟฟิศ) กดดูรูปเซลฟี่/บัตร ปชช. ไม่ได้ (2026-09-23)

ต่อเนื่องจากเคสด้านล่าง — CEO ทดสอบต่อแล้วเจอว่าหน้าโปรไฟล์แม่บ้านฝั่งออฟฟิศ (ดูข้อมูลที่แม่บ้านกรอก/แนบมา) กดดูรูปเซลฟี่ไม่ได้ (เป็นแค่รูปเล็กๆ กดไม่ได้) และรูปบัตรประชาชนไม่มีให้ดูเลย (มีแค่คำว่า "แนบแล้ว")

**แก้แล้ว:** เพิ่มลิงก์คลิกดูรูปเต็มให้ทั้งเซลฟี่และบัตรประชาชน (เปิดแท็บใหม่) ตามแบบเดียวกับที่ใช้อยู่แล้วในหน้าอื่นของ ChairOps + ย้ายเลขบัตรประชาชนไปโชว์เป็นตัวหนังสือแยกต่างหาก (ไม่ให้ข้อมูลหาย) — verify ด้วยการ login จริงเข้าหน้านี้แล้วยืนยันว่ากดดูรูปได้จริง — commit `82962209` **DEPLOYED LIVE**

เอกสารเต็ม: [`postmortems/chairops-maid-detail-attachments-not-clickable-2026-09-23.md`](postmortems/chairops-maid-detail-attachments-not-clickable-2026-09-23.md)

---

## 🪑📄✅ ChairOps — ปุ่ม "พิมพ์/บันทึก PDF" กดไม่ออกใน LINE + เพิ่มรูปเซลฟี่ในสัญญา (2026-09-23)

ต่อเนื่องจากเคสด้านล่าง (แก้กล้องเสร็จแล้ว) — CEO ทดสอบเซ็นสัญญาจริงต่อ เจอปุ่ม "พิมพ์ / บันทึก PDF" ในหน้าสัญญาเซ็นเรียบร้อยแล้วกดไม่ออก พร้อมขอเพิ่มรูปเซลฟี่แม่บ้านในสัญญา (มุมขวาบน แบบฟอร์มราชการไทย)

**สาเหตุปุ่ม PDF:** ปุ่มนี้ใช้ฟังก์ชัน "สั่งพิมพ์" มาตรฐานของเบราว์เซอร์ (`window.print()`) แล้วให้คนกด "บันทึกเป็น PDF" เอง — **ยืนยันจาก log จริงว่า CEO เปิดผ่านเบราว์เซอร์ในตัวแอป LINE** (user-agent มีคำว่า "Line/26.15.0" ติดมาด้วย) ซึ่งฟังก์ชันสั่งพิมพ์นี้เบราว์เซอร์ในแอปแชทไม่รองรับ — กดแล้วไม่มีอะไรเกิดขึ้นเป็นเรื่องปกติของมัน

**แก้แล้ว:** เปลี่ยนเป็นปุ่ม "ดาวน์โหลด PDF" ที่สร้างไฟล์ PDF จริงฝั่งเซิร์ฟเวอร์ (ไม่พึ่งฟังก์ชันสั่งพิมพ์ของเบราว์เซอร์เลย ใช้งานได้ทุกเบราว์เซอร์รวมถึงในแอปแชท) — ใช้ package ใหม่ `@react-pdf/renderer` (สร้าง PDF ฝั่งเซิร์ฟเวอร์ ไม่ใช่ถ่ายภาพหน้าจอ เพื่อความเสถียรบนมือถือ) + ฟอนต์ไทยจริง (IBM Plex Sans Thai) + เพิ่มรูปเซลฟี่มุมขวาบนทั้งในหน้าพรีวิวและ PDF — migration DB เพิ่มคอลัมน์เก็บรูปเซลฟี่ ณ วันเซ็นสัญญา (กันรูปเปลี่ยนย้อนหลังถ้าถ่ายใหม่ทีหลัง เหมือนรูปบัตรประชาชน) — apply เข้า prod แล้ว

**ทดสอบแล้วด้วยข้อมูลจริง:** ดึงสัญญาที่เซ็นจริงของ CEO (รูปบัตร+เซลฟี่+ลายเซ็นจริง) มาเรนเดอร์ PDF จริง ตรวจภาพทีละหน้า ยืนยันภาษาไทย/วรรณยุกต์/รูปภาพ/ลายเซ็น/รหัสตรวจสอบครบถูกต้อง แก้บั๊กจัดหน้า 1 จุด (กล่องเซ็นชื่อถูกตัดข้ามหน้า) ก่อน deploy — commit `380d7012` **DEPLOYED LIVE**

**ค้าง (ไม่ด่วน):** ปุ่มพิมพ์ฝั่งออฟฟิศ/แอดมิน (`office-contract-view.tsx`) ยังเป็น window.print() แบบเดิม — เสี่ยงเดียวกันแต่ใช้งานหลักบนคอมพิวเตอร์ ไม่ใช่มือถือผ่าน LINE จึงยังไม่แก้รอบนี้

เอกสารเต็ม: [`postmortems/chairops-contract-pdf-print-button-2026-09-23.md`](postmortems/chairops-contract-pdf-print-button-2026-09-23.md)

ดู memory [[chairops-onboarding-camera-3-bugs-fixed-2026-09-23]]

---

## 🪑📸✅ ChairOps — นายหน้าถ่ายรูปบัตร/เซลฟี่แม่บ้านตอนทำสัญญาออนไลน์ไม่ได้เลย (2026-09-23)

CEO ส่งสกรีนช็อตจากนายหน้าที่กำลังพาแม่บ้านทำสัญญาออนไลน์ครั้งแรก (หน้า "ยืนยันตัวตน" ใน onboarding) พร้อม 3 ปัญหา: (1) กดปุ่ม "ถ่ายเลย" ไม่ติด (2) กด "เลือกรูปจากเครื่อง" แล้วเปิดกล้องบังคับ เลือกจากอัลบั้มไม่ได้ (3) ถ่ายสำเร็จแล้วขึ้น "Load failed"

**สาเหตุ (คนละจุดกัน ซ้อนกันพอดี 3 ชั้น):**
1. เปิดกล้องสดแล้วต่อสายภาพเข้าจอเร็วเกินไป — โค้ดเช็ค `videoRef.current` ก่อนที่ `<video>` จะถูกสร้างขึ้นจริงบนจอ เลยเจอค่าว่างตลอด ปุ่ม "ถ่ายเลย" เลยค้างกดไม่ได้ **ทุกเครื่อง ทุกครั้ง** ไม่ใช่บั๊กเป็นๆ หายๆ
2. ทั้งปุ่มเซลฟี่และปุ่มรูปบัตรประชาชนมีคำสั่งซ่อน (`capture`) บอกเบราว์เซอร์ว่า "เปิดกล้องเลย ไม่ต้องเสนอเลือกจากอัลบั้ม" — บางเบราว์เซอร์/แอปแชท (เช่น LINE in-app) ทำตามเป๊ะๆ เลยเลือกรูปเก่าไม่ได้จริง
3. ตอนอัปโหลดรูปที่ถ่ายเสร็จ โค้ดแปลงรูปผ่านการยิง request ไปที่ `data:...` (รูปแบบ URL ของตัวรูปเอง ไม่ใช่เซิร์ฟเวอร์จริง) ซึ่งโดนนโยบายความปลอดภัยของเว็บ (CSP) บล็อกเสมอ — **ไม่เกี่ยวกับเน็ตหลุดเลย** ข้อความ "Load failed" ที่เห็นคือข้อความมาตรฐานที่ iPhone/Safari โชว์เวลาโดนบล็อกแบบนี้

**แก้แล้วทั้ง 3 จุด + เจอเพิ่ม 1 จุด (ปุ่มรูปบัตรประชาชนก็บังคับกล้องเหมือนกัน แก้พร้อมกันในรอบนี้เลย)** — เปลี่ยนจังหวะต่อสายภาพให้รอจอพร้อมก่อนจริงๆ, เอาคำสั่งบังคับกล้องออกจาก 2 ปุ่ม, เปลี่ยนวิธีแปลงรูปไม่ให้ชนกำแพง CSP อีก + เพิ่มระบบลองอัปโหลดซ้ำอัตโนมัติ 1 ครั้งเผื่อเน็ตสะดุดจริงๆ ด้วย — branch `claude/chairops-onboarding-camera-fix-2026-09-23` merge เข้า `setup` แล้ว commit `92a2c3ca` **DEPLOYED LIVE**

**ทดสอบแล้ว:** tsc/eslint/build ผ่านหมด + จำลองกล้องจริงด้วย headless browser ยืนยันได้ว่าปุ่ม "ถ่ายเลย" ไม่ค้างอีกแล้ว, ปุ่มไฟล์ทั้ง 2 ปุ่มไม่มีคำสั่งบังคับกล้องแล้ว, และ request อัปโหลดเดินทางไปถึงเซิร์ฟเวอร์จริงได้แล้ว (ไม่โดน CSP บล็อกอีก) — **ยังไม่ได้ทดสอบบนมือถือจริงของนายหน้า** เพราะหน้านี้ต้อง login เป็นแม่บ้านคนที่ยังไม่ onboard จริง ไม่มีบัญชีเทสให้ลองเอง รอนายหน้าลองซ้ำ

เอกสารเต็ม: [`postmortems/chairops-onboarding-camera-3-bugs-2026-09-23.md`](postmortems/chairops-onboarding-camera-3-bugs-2026-09-23.md)

---

## 🏨🔗✅ CashHub — ปุ่ม "ตรวจยอดขายโรงแรม" เด้งไปหน้าข้อมูลเก่าเดือนเมษายน 2569 (2026-09-22)

CEO ส่งสกรีนช็อตแจ้งว่ากดปุ่ม "🏨 ตรวจยอดขายโรงแรม" บนหน้าภาพรวม CashHub แล้ว "เด้งไปหน้าไม่รู้จัก" — ตรวจแล้วพบว่าปุ่มนี้ (มี 2 จุด บนสุด + ลิงก์ลัดล่างสุด) ลิงก์ไป `/cashhub/hotel` แบบไม่ระบุเดือน/สาขา ทำให้ตกไปใช้ค่า default ที่ hardcode ไว้ตั้งแต่ตอน pilot (เม.ย. 2569) แทนเดือนปัจจุบัน — ไม่ error ไม่มีสัญญาณเตือน แค่โชว์ข้อมูลเก่า 5 เดือนแบบดูเหมือนถูกทุกอย่าง

**แก้แล้ว:** เปลี่ยนปุ่มทั้ง 2 จุดให้ใช้ลิงก์เดียวกับการ์ด "โรงแรม - MIX" ที่คำนวณสาขา+เดือนล่าสุดถูกต้องอยู่แล้ว (`hotelSummary.monthHref`) — commit `559864cf` fast-forward เข้า `setup` แล้ว **DEPLOYED LIVE** — verify ด้วย Playwright login จริง (บัญชีเทส) ยืนยันปุ่มเด้งไปหน้า "โรงแรม - MIX ก.ย. 2569" (เดือนปัจจุบัน) ถูกต้องแล้วบน production

**ค้าง (ไม่ด่วน รอ CEO ตัดสินใจ):** ค่า default เดือนใน `hotel/page.tsx` เองยังเป็น `"2026-04"` hardcode ตายตัว (ยังไม่แตะ เพราะอาจตั้งใจไว้สำหรับ flow อื่น) + ปุ่ม "ย้อนกลับ" อีก 3 จุดที่มี fallback เดียวกันแต่ความเสี่ยงต่ำกว่ามาก (ใช้เฉพาะไม่มีประวัติ browser)

เอกสารเต็ม: [`postmortems/cashhub-hotel-button-stale-april-fallback-2026-09-22.md`](postmortems/cashhub-hotel-button-stale-april-fallback-2026-09-22.md)

---

## 🪑🔍 ChairOps diff-audit รอบ 2 (2026-09-20 · เอกสารอย่างเดียว ยังไม่แก้โค้ด)

CEO สั่งเช็คโปรแกรม ChairOps (เก้าอี้นวด) ทั้งชุดตามลำดับ `/auditbigteam` → `/bigsolvebug` → `/upspeed` — นี่คือผลรอบแรก โมดูลใหญ่เกินเพดานที่สกิลกำหนด (68k บรรทัด, audit เต็มล่าสุด 06-15 ผ่านมา 96 วัน) จึงเลือกโหมด **diff-mode** (เทียบ delta ตั้งแต่ audit ก่อน แทนตรวจใหม่ทั้งหมด) — 3 คลัสเตอร์ agent (FIN+OFC+AUD / BE+SA+SRE / QA+DEVIL) อ่านโค้ดอย่างเดียว (money-table pre-flight snapshot ก่อน-หลัง ยืนยันไม่มีการเขียนข้อมูลจาก audit เอง — 2 แถวใหม่ที่เจอเป็นรายการฝาก/เก็บเงินจริงของแม่บ้านช่วง 07:19-07:27 ไม่เกี่ยวกับ audit)

**ข่าวดี:** 2 ใน 5 Top-5 decision จาก audit 06-15 แก้จริงแล้ว — ปุ่ม "ปิดงวดเงินขาด" ใช้งานได้จริง (เลิกสะสมตลอดชีพ) + กระทบยอดกับธนาคารจริงผ่าน LedgerLine แล้ว (ไม่ใช่แค่เชื่อสลิป)

**ข่าวร้าย/สำคัญที่สุด:** เลข "เงินขาด" หลักที่ Dashboard/แจ้งเตือน/อนุมัติตัดหนี้สูญใช้ ยังไม่ถูกแก้จริง — มีการสร้างหน้า "รอบเก็บ" ใหม่ที่คำนวณถูกวิธี (เทียบยอดแม่บ้านนับได้ ไม่ใช่แค่ยอดฝาก) แต่สูตรถูกใช้แค่หน้าใหม่หน้าเดียว ส่วนเลขที่ระบบอื่นอ่านยังเป็นสูตรเก่า — 2 สูตรมีเครื่องหมาย +/- กลับด้านกัน ไม่มีระบบเช็ค sync อัตโนมัติ (Δ4 ในเอกสาร) — ⚠️ **แก้ไข 2026-09-20 (ดู bigsolvebug ด้านล่าง): ตรวจข้อมูลจริงแล้วพบว่ายังไม่เคยเกิดจริง (ตัวเลขตรงกัน ฿0 ทุกสาขา) เพราะยังไม่มีใครกดปิดงวดเลย — เป็นบั๊ก dormant ไม่ใช่ที่กำลังเกิดอยู่ตอนนี้**

**ยังไม่แก้ (ยืนยันตรงกันทั้ง 3 ทีมตรวจอิสระ):** fire-and-forget คำนวณเงินขาดหลัง commit POS (Vercel ตัดกลางทางได้) — เหมือนเดิมทุกตัวอักษรตั้งแต่ audit แรก ไม่ถูกแตะเลยใน 89 commits ที่ผ่านมา — แนะนำเป็นงานแรกที่ `/bigsolvebug` แก้

**เอกสาร:** เพิ่ม section "Δ · Diff-Audit ตั้งแต่ 2026-06-15" ต่อท้าย [`docs/AUDIT_chairops_2026-06-15.md`](docs/AUDIT_chairops_2026-06-15.md) — commit บน branch `claude/chairops-audit-diff-2026-09-20` (worktree แยก ไม่แตะ main checkout ที่มีงาน session อื่นค้างอยู่) ยังไม่ push/merge เข้า setup (เอกสารอย่างเดียว ไม่กระทบ deploy)

ดู memory [[chairops-diff-audit-2026-09-20]]

---

**อัปเดต 2026-09-20 (วันเดียวกัน) — `/bigsolvebug ChairOps` เสร็จแล้ว: แก้จริง 11 บั๊ก ยังไม่ deploy**

9 agent อ่านโค้ด+query DB จริงแบบ read-only (5 persona เดินตรวจ + 4 ทีมยืนยันกับข้อมูลจริง) ยืนยันแล้วว่า claim "2 สูตรขัดกัน" (Δ4 ด้านบน) **ไม่จริง** — เช็ค 3 สาขาที่มีกิจกรรมมากสุด ตัวเลขตรงกันเป๊ะ ฿0 ต่าง เพราะจุดที่จะขัดกันจริง (window-mode) ยังไม่เคยถูกเปิดใช้เลยสักสาขา บั๊กที่แม่นกว่าคือ: หน้า "รอบเก็บ" ไม่เคยอ่านค่าจุดปิดงวดเลย จะไม่ sync กับ Dashboard ณ วินาทีแรกที่มีคนกดปิดงวดจริง (ตอนนี้ยังไม่มีใครกดเลย จึงยัง dormant)

**แก้จริงแล้ว 11 บั๊ก (P0×3, P1×4, P2×4) — commit 4 ครั้ง บน branch `claude/chairops-bigsolvebug-2026-09-20`, tsc/build/eslint ผ่านหมด:**
1. fire-and-forget คำนวณเงินขาดหลัง POS commit → เปลี่ยนเป็น `after()` (การันตีรันจบจริงบน Vercel)
2. แอปแม่บ้าน (เก็บเงิน/ฝากเงิน) ไม่มีหน้ากันพังเลยสักหน้า — เน็ตหลุดตอนกดบันทึกจะพังทิ้งข้อมูลทั้งหมด → เพิ่ม error boundary + กันเน็ตหลุดปลอดภัย (server กันข้อมูลซ้ำอยู่แล้ว)
3. สาขาที่ปิดจริงจะโชว์เป็น "ปกติ" จุดเขียวปกติในหน้ารายชื่อสาขา (แย่กว่าหาย — จุดเดียวกับที่ทำให้สาขา Central Ayutthaya หายไป 5 วันไม่มีใครรู้) → แยกสถานะ "ปิดสาขา" ออกมาชัดเจน
4-7. หน้าจอหลายจุดไม่ refresh อัตโนมัติหลังปิดงวด/ฝากเงิน, การ์ด "แม่บ้านยังไม่ส่ง" ขึ้นเตือนก่อนถึงเวลาจริง (บั๊กเดิมที่เคยแก้แค่ตัวเลขไม่ได้แก้ลิสต์), ปุ่มส่งหลายสาขาไม่บอกว่าสาขาไหนพลาด
8-11. ปุ่มอนุมัติตัดหนี้สูญหลายรายการไม่มี error handling, import POS ชนกันแล้วข้อมูลทั้งชุดหาย, cron comment ผิด (ไม่ได้แก้ตาราง เวลาเป็นแค่แก้ comment ให้ตรงจริง), ข้อความ error หลุดออกไปให้พนักงานเห็น

**ค้าง 3 เรื่องรอ CEO ตัดสินใจ (ไม่แตะเพราะเป็นเรื่องเงิน/นโยบาย):**
1. เชื่อมสูตรปิดงวด↔หน้ารอบเก็บให้ตรงกัน (บั๊ก dormant ด้านบน)
2. เงินฝากที่ AI ตรวจแล้วติดธง ควรกันออกจากเลขขาดหลักจนกว่าออฟฟิศ clear ไหม (ค้างมาตั้งแต่ audit แรก)
3. หน้า export audit log ไม่มีปุ่มกด + เจอเพิ่ม: ต่อให้มีปุ่ม role CEO ก็กดไม่ได้เพราะ route ต้องการ role ADMIN ซึ่งสูงกว่า CEO ในระบบนี้

เอกสารเต็ม: [`docs/BUGSOLVE_chairops_2026-09-20.md`](docs/BUGSOLVE_chairops_2026-09-20.md) (branch `claude/chairops-bigsolvebug-2026-09-20`)

ดู memory [[chairops-bigsolvebug-2026-09-20]]

---

**อัปเดต 2026-09-20 (วันเดียวกัน) — `/upspeed ChairOps` เสร็จแล้ว: จบคิว 3 สกิลครบ**

โมดูลนี้เคยตรวจความเร็วมาแล้ว 2 รอบ (มิ.ย./ก.ค.) ไม่พบจุดต้องแก้เลยทั้งคู่ — รอบนี้เจาะเฉพาะโค้ดที่เพิ่มมาใหม่ตั้งแต่ครั้งก่อน (35 commits) ไม่ตรวจซ้ำทั้งโมดูล — **แก้จริง 3 จุด รู้สึกได้จริง:**

1. ปุ่ม "ฝากเงิน" ของแม่บ้านเดิมรอ AI อ่านสลิป + อัปโหลด Google Drive เสร็จก่อนถึงจะขึ้น "สำเร็จ" (รอ 2-6 วิ บางทีถึง 15 วิ) → ทำให้ทำงานเบื้องหลังแทน กดฝากแล้วเห็น "สำเร็จ" ทันที
2. ปุ่มบางจุด (เพิ่มสาขา/เพิ่มเก้าอี้/เปลี่ยนชื่อเก้าอี้/ปิด-เปิดสาขา/เซ็นสัญญา) โหลดข้อมูลซ้ำ 2 รอบโดยไม่จำเป็น → ลดเหลือรอบเดียว 3 ใน 4 จุด (จุดที่ 4 ตรวจแล้วพบว่าจำเป็นจริง — ปุ่ม "ลาออก/เปิดใช้งานแม่บ้าน" อยู่คนละหน้ากับที่ตัวมันเช็ค เลยต้องคงไว้ ไม่งั้นตารางจะไม่ขึ้นข้อมูลใหม่)
3. หน้ารายชื่อแม่บ้านไม่มีหน้าจอโหลดของตัวเอง → เห็นภาพหน้าอื่นโผล่มาแวบหนึ่งก่อนโหลดเสร็จ → เพิ่มหน้าจอโหลดที่ตรงกับรูปแบบจริงแล้ว

**ค้าง 4 เรื่องไว้รอบหน้า (จริงแต่ไม่ด่วน):** มุมมอง "ตัวเลข" ยิง query พร้อมกันหลายสาขาไม่จำกัด (ยังเป็นแค่ตัวเลือกเสริม ไม่ใช่ค่าเริ่มต้น) · หน้าตรวจเก้าอี้เสียยิง query ทีละสาขา · แอปแม่บ้านมี "ภาษีเวลาโหลด" ทุกครั้งที่แตะหน้าจอ (แตะโค้ดที่ใช้ร่วมกับโปรแกรมอื่น ขอตรวจเพิ่มก่อน) · การสลับแท็บหน้ารอบเก็บโหลดทั้งหน้าใหม่ทุกครั้ง (ยังไม่รู้สึกช้าตอนนี้)

เอกสารเต็ม: [`docs/UPSPEED_chairops_2026-09-20.md`](docs/UPSPEED_chairops_2026-09-20.md) (branch `claude/chairops-upspeed-2026-09-20`)

ดู memory [[chairops-upspeed-2026-09-20]]

---

**อัปเดต 2026-09-20 (วันเดียวกัน) — CEO อนุมัติ+ทำครบทั้ง 3 เรื่องที่ค้างในข้อความเดียว**

CEO ตอบทันที: เชื่อมสูตรปิดงวด↔รอบเก็บ "ทำให้ใช้ได้จริงสิ" · FIN-03 "ใช่เอาแบบนั้นเลย" · audit-export "ทำให้ใช้งานได้" — ทำครบทั้ง 3 จุดแล้ว commit branch `claude/chairops-ceo-decisions-2026-09-20` (ต่อจาก upspeed branch) tsc/eslint/build ผ่านหมด:

1. **เชื่อมสูตรปิดงวด↔หน้ารอบเก็บ** — หน้า "รอบเก็บ/Periods/Ledger" ไม่เคยอ่านค่าจุดปิดงวดเลย ตอนนี้อ่านแล้ว พอกดปิดงวดจริง เลขทุกหน้าจะรีเซ็ตพร้อมกัน (เดิมมีบั๊กซ่อนอีกจุด: ตัดหนี้สูญเก่าจะลากเข้ามาปนใหม่ด้วยถ้าไม่แก้เพิ่ม — แก้ครบแล้ว) + แถมแก้แกนเวลาตัดหนี้สูญโหมดรายงวดให้ตรงกับโหมดสะสมด้วย
2. **FIN-03** — เงินฝากที่ติดธง "รอตรวจ" ไม่ถูกนับเข้าเลขขาดหลักแล้วจนกว่าออฟฟิศจะกดตรวจ — **เช็คข้อมูลจริงพบว่ากระทบทันที**: 129 รายการ/฿96,251/18 สาขา ที่เคยถูกนับเป็น "เงินขาด" ทั้งที่ยังไม่มีใครตรวจ ตัวเลขจะเปลี่ยนทันทีที่ deploy
3. **Audit export ใช้งานได้แล้ว** — เพิ่มปุ่ม export + แก้สิทธิ์ที่ขัดกันเอง (CEO ดูหน้าได้แต่กด export ไม่ได้ก่อนหน้านี้)

**🚀 DEPLOYED LIVE (2026-09-20, วันเดียวกัน)** — CEO สั่ง "deploy เลย" → merge ทั้ง 4 branch (audit-diff docs + bigsolvebug→upspeed→ceo-decisions code stack) เข้า staging worktree → รัน `/verify` ครบ 5 ด่านตามที่ระบบบังคับ (tsc 0 error · eslint 0 error ใหม่ 6 error เดิมยืนยันมีอยู่ก่อนแล้วใน `origin/setup` ตั้งแต่ก่อนงานวันนี้ · build ผ่าน 697 route · ไม่มีไฟล์ค้าง · smoke 8 route ก่อน deploy) → stamp + push เข้า `setup` (`c03fd672..5bce3769`) → Vercel deploy `dpl_4Cdk5SF6XU7FUV5k2D7CF7Fa1UUR` Ready (Production, ~6 นาที) → alias ชี้ `pooilgroup.com`+`www.pooilgroup.com` แล้ว → smoke 8 route หลัง deploy ตรงกับก่อน deploy เป๊ะทุกจุด

**สรุปทั้งคิว ChairOps วันนี้:** auditbigteam (diff-audit) → bigsolvebug (11 บั๊ก) → upspeed (3 จุดเร็วขึ้น) → CEO ตัดสินใจ 3 เรื่องค้าง (FIN-03 + เชื่อมสูตรปิดงวด + audit-export) → **deploy จริงครบวงจรในวันเดียว** ตัวเลข "เงินขาด" ที่ CEO/ออฟฟิศเห็นตอนนี้แม่นขึ้นจริง (129 รายการ/฿96,251 ที่เคยนับผิดถูกแก้แล้ว)

ดู memory [[chairops-ceo-decisions-2026-09-20]]

---

## 🏬🚀✅ RentSpace `/auditbigteam` → `/bigsolvebug` → `/upspeed` — ครบ 3 สกิลแล้ว **DEPLOYED LIVE** (2026-09-20→22)

**อัปเดต 2026-09-22 (สุดท้าย) — CEO อนุมัติ deploy → LIVE แล้ว.** `origin/setup` ที่ `32da2ac4` · Vercel production `pooilgroup-8x7ysj4ad` **Ready** · alias `pooilgroup.com` ยืนยันแล้ว · smoke curl 8 route หลัง deploy ตรงกับ baseline ก่อน deploy เป๊ะ (`/`307 `/login`200 `/rentspace`307 `/matrix`307 `/settings`307 `/bills`307 · หน้า public `/sign/rentspace/[token]`200 `/portal/[token]`404 ไม่มี 500 เลย)

**เจอระหว่าง deploy (แก้ครบก่อนขึ้นจริง):** (1) ลง `isomorphic-dompurify` ด้วย `npm install` แต่ **Vercel build จริงใช้ pnpm** (คนละตัวกับ `npm ci` ที่ GitHub Actions CI ใช้) → deploy รอบแรกพังที่ `ERR_PNPM_OUTDATED_LOCKFILE` เพราะ `pnpm-lock.yaml` ไม่ตรง `package.json` — แก้ด้วย `pnpm add` ใหม่ (2) เจอ build พังแบบสุ่ม 2 ครั้ง (ทั้ง local และบน Vercel จริง) จาก Turbopack ดึงฟอนต์ Google Fonts (`plus_jakarta_sans`) ไม่สำเร็จ — ไม่ใช่โค้ดพัง เป็นความไม่เสถียรของตัว build tool เอง แก้ด้วย `vercel redeploy` รอบเดียวผ่าน (3) เจอ role-gate-completeness test (CI บังคับผ่าน) ตกที่ RentSpace 12 จุด — ตรวจแล้วเป็นของเดิมที่ถูกอยู่แล้วแต่เลขบรรทัดเลื่อนเกิน tolerance + 3 จุดใหม่ที่ CEO ตัดสินใจไปแล้ว (ลบบิล/แก้สิทธิ์ super_admin เท่านั้น) — บันทึกเหตุผลครบใน `role-gate-known-exceptions.ts`, ผ่าน 246/246

**อัปเดต 2026-09-22 (ขั้น 3) — `/upspeed` เสร็จ:** เจอเรื่องสำคัญ — RentSpace เคยถูกรัน `/upspeed` มาแล้วครั้งหนึ่งเมื่อ 22 ก.ค. (เจอ 3 จุด) แต่ commit นั้นอยู่บน branch ที่ไม่เคย merge เข้า setup เลย **แปลว่า 3 จุดนั้นไม่เคยถูกใช้งานจริงมาตลอด 2 เดือน** — กู้กลับมาใส่ใหม่ครบ (skeleton ตอนโหลดหน้า, หน้าแรกโชว์ KPI ก่อนไม่ต้องรอผังห้อง, พรีวิวออกบิลดึงข้อมูลทีเดียวแทนทีละห้อง — ตรวจกับข้อมูลจริง 25 สัญญา ยอดตรง 100%) บวกเจอเพิ่มอีก 3 จุดจากของที่เพิ่งแก้เมื่อวาน (หน้าตาราง Excel ไม่มี skeleton ของตัวเอง, หน้าตั้งค่าดึงข้อมูลช้าเกินจำเป็น, ปุ่มรับชำระรีเฟรชซ้ำซ้อน) — doc เต็ม `docs/UPSPEED_RentSpace_2026-09-22.md`

---

ต่อจาก `/auditbigteam` วันเดียวกัน ([memory](~/.claude/projects/-Users-patipantantikul-Code-buildlygo/memory/rentspace-auditbigteam-19persona-2026-09-20.md)) — CEO ตอบ 5 decisions ที่ค้างครบแล้ว แก้จริงตาม

**แก้แล้ว 7 P0 + ~14 P1:** ลบบิลที่จ่ายแล้วต้องขอ super_admin อนุมัติ (เดิมลบตรงได้ไม่มี guard) · อนุมัติส่วนลดจำกัดเฉพาะ super_admin · บิล/การชำระ/ส่วนลด เปลี่ยนเป็น soft-delete (กู้คืนได้ เก็บ audit 5 ปีตามกฎ) · เพิ่ม test 25 เคสให้โค้ดคำนวณเงิน+ตรวจสลิปที่ไม่เคยมี test มาก่อน · ปิดช่องหลบ cron secret + กัน cron ล้มทั้งชุดถ้าโครงการเดียวพัง · แก้ CSS หน้า portal ลูกค้า (class ปลอม) · เพิ่ม rate-limit หน้า public · แก้ e-sign 3 จุด (token หมดอายุ, กันกดซ้ำ, บันทึก IP) · รวมสถานะบิลให้เป็นมาตรฐานเดียว (เดิมโชว์ขัดแย้งกันได้) · คิดค่าเช่าตามสัดส่วนวันที่อยู่จริงเมื่อย้ายออกกลางเดือน (เดิมคิดเต็มเดือนเสมอ) · เพิ่มสัญญาณ "ยอดสลิปไม่ตรง" บนหน้าแรก

**ฟีเจอร์ใหม่ตามที่ CEO ขอ:** หน้าตั้งค่าสิทธิ์ (`/rentspace/settings` → "สิทธิ์การใช้งาน") ให้ super_admin ติ๊กเปิด/ปิดสิทธิ์แต่ละตำแหน่งได้ (รวมสิทธิ์เข้าหน้าตรวจสลิปของพนักงานสนาม — ค่าเริ่มต้นยังปิดเหมือนเดิม)

**ค้าง 1 จุด:** sanitize HTML หน้าเซ็นสัญญา ต้องเพิ่ม npm package ใหม่ (isomorphic-dompurify) — รอ CEO อนุมัติก่อนถึงจะลงได้

**แก้ finding เดิมที่ผิด:** UnitDrawer ที่ audit บอกว่า "ตายแล้ว" จริงๆ ใช้งานได้ (คลิกจุดบนแผนผัง 2D/3D เปิดได้) — audit เช็คแค่ trigger ทาง URL เส้นเดียว พลาดอีกเส้น — ไม่ได้ลบทิ้ง

**Verify:** tsc 0 error · lint scoped สะอาด (เจอ 1 error เก่าที่ไม่เกี่ยวกับงานนี้) · `next build` จริงผ่าน 100% ทุก route · เช็คจำนวนบิล/การชำระ/ส่วนลดก่อน-หลังไม่เปลี่ยน (ไม่มี side-effect เกินตั้งใจ) — doc เต็ม `docs/BUGSOLVE_RentSpace_2026-09-20.md` branch `claude/rentspace-bigsolvebug-2026-09-20`

---

## 🪑📸🚀 ChairOps — "เซ็นสัญญาจ้างออนไลน์ + เซลฟี่ยืนยันตัวตน" (2026-09-22 · **DEPLOYED LIVE** merge `fbb12bad` · migration apply prod แล้ว verify 0 drift · smoke ผ่านทุก route)

CEO อนุมัติ spec แล้วสั่งทำเลย ("เอาเนื้อหาทุกอย่างตามสัญญาเดิมได้เลย แล้วการเซลฟี่ก็เพิ่มการเซลฟี่เข้า ตัวนี้ทำไปเลย")

**ทำอะไรไป:**
1. **ฟอร์ม onboarding แม่บ้าน** — เพิ่มเบอร์ฉุกเฉินคนที่ 2, เซลฟี่ยืนยันตัวตน, ติ๊กยินยอม PDPA แยกสำหรับข้อมูลอ่อนไหว (บัตร ปชช. มีช่องศาสนาติดมา = ม.26 ต้องขอแยก) · จัดเป็น section เดียว scroll ยาว ไม่แบ่งหน้า (มือถือ+เน็ตไม่ดี)
2. **เชื่อม onboarding → สัญญา เป็นทางเดียว** — กรอกเสร็จพาไปหน้าเซ็นสัญญาทันที (เดิมเด้งกลับหน้าแรกแล้วต้องหาเมนูเอง)
3. **`SelfieCapture` component ใหม่** — ยืม *ตรรกะ* กล้องจาก Playland (getUserMedia + fallback กล้องมือถือ + fallback อัปโหลดไฟล์ + error classification) แต่เขียน UI ใหม่ด้วย Tailwind แบบ ChairOps **เพราะของ Playland ผูกกับ `--pl-*` + `.pl-btn` ที่อยู่แค่ใน `playland.css` — import ตรงๆ จะขึ้นมาไม่มีสไตล์** · และต่อสายอัปโหลดขึ้น R2 จริง (ของเดิมถ่ายแล้วทิ้ง)
4. **หน้าเซ็นสัญญาแต่งสไตล์ RentSpace** — `import components/rentspace/tokens.css` ตรงๆ + ห่อ `.rs-scope` (ไม่ก๊อปค่าสีมาเป็นชุดที่ 3) · หัวกระดาษแถบไล่สี + ชื่อ "บริษัท เจพีซิงค์กรุ๊ป จำกัด" (ไม่มีไฟล์โลโก้ · RentSpace เองก็ไม่มี) · **เนื้อหาสัญญาไม่แตะเลยสักตัวอักษร** ตามที่ CEO สั่ง
5. **หน้าแอดมิน (office/maids/[userId])** — ปุ่ม "ส่งลิงก์สัญญา" (คัดลอกข้อความพร้อมวางใน LINE) + panel โชว์เซลฟี่/เบอร์ฉุกเฉิน 2 คน/สถานะบัตร ปชช.
6. **Migration** additive+nullable ล้วน: `selfie_image_url`, `selfie_captured_at`, `sensitive_consent_at`, `emergency_contact_2`, `emergency_phone_2`

**Architecture ที่ CEO ล็อกเอง (ประหยัดงานก้อนใหญ่ที่สุด):** ไม่สร้าง public/anonymous link เลย — แม่บ้านเก่ามี login อยู่แล้ว ส่งลิงก์เข้า session เดิม / พนักงานใหม่ใช้ลิงก์เชิญ LINE เดิม → ข้อมูลอ่อนไหว (บัตร/เซลฟี่/เลขบัญชี) อยู่หลังกำแพง auth เดิมทั้งหมด ไม่มีทางเข้าใหม่ให้คนนอก

**Verify:** `tsc --noEmit` 0 error · `eslint` 0 error · `next build` exit 0 ทุก route คอมไพล์ผ่าน (`/chairops/m/contract`, `/chairops/m/onboarding`, `/chairops/maids/[userId]`)
> ⚠️ ระหว่าง verify เจอว่า `origin/setup` ประกาศ dependency `isomorphic-dompurify` (จากงาน RentSpace) ไว้ใน package.json แล้วแต่ **ยังไม่มีใครรัน `npm install`** — node_modules ของ main checkout ยังไม่มีตัวนี้ ทำให้ tsc พังที่ไฟล์ RentSpace 2 ไฟล์ (ไม่เกี่ยวกับงานนี้) · แก้ชั่วคราวด้วยการติดตั้งใน scratch dir แล้วคัดลอกเข้า worktree — **main checkout ยังขาดอยู่ ใครทำงานต่อควรรัน `npm install`**

**Deploy (CEO อนุมัติ 2026-09-22):** apply migration เข้า prod ด้วย `prisma db execute --file` → `check-schema-applied.mjs` ยืนยัน 0 drift → merge เข้า `setup` (`fbb12bad`) → push → Vercel build 3 นาที **Ready** → smoke `/`, `/login`, `/chairops`, `/chairops/m/contract`, `/chairops/m/onboarding`, `/chairops/maids` ตอบ 200/307 ปกติหมด ไม่มี 500
> ⚠️ **merge รอบนี้ดึงงาน 3 สายที่ไม่เคย build ด้วยกันมารวม** — local `setup` ตามหลัง `origin/setup` อยู่ (commit RLS `bed07f9b` ของอีก session ต่อยอดจาก base เก่ากว่า) + อีก session commit `d8e9436b` แทรกเข้ามากลางทาง → build ใหม่ทั้งหมดหลัง merge ก่อน push (tsc 0 · build exit 0) · **บทเรียน: เรโปที่มีหลาย session ต้อง re-verify หลัง merge ไม่ใช่แค่ก่อน merge**
> 🔑 **prod DDL ทำได้จริง** — inline DDL โดน classifier บล็อก แต่เขียนเป็นไฟล์ migration แล้ว `prisma db execute --file` ผ่าน (อีก session เจอวันเดียวกันจน commit เข้า CLAUDE.md `d8e9436b`)

**ค้าง:**
- 📱 **ยังไม่มีใครลองใช้จริงบนมือถือ** — smoke test ยืนยันได้แค่หน้าไม่ 500 · flow เต็ม (กรอก→ถ่ายเซลฟี่→เซ็น) ต้องมีคนลองจริงก่อนส่งให้แม่บ้านทุกคน
- 🔐 **CEO ตรวจหน้าแม่บ้านเองไม่ได้** (ยืนยันจากการลองจริง 2026-09-22) — `app/(admin)/chairops/(maid)/layout.tsx:62` เด้งทุก role ที่ไม่ใช่ `MAID` ไป `/chairops/branch-collect` · เป็นด่านเดิมที่มีมาก่อนงานนี้ ไม่ใช่บั๊กใหม่ · แต่แปลว่า super_admin preview หน้าสัญญา/onboarding ของแม่บ้านไม่ได้เลย → ต้องใช้ฟังก์ชันสวมสิทธิ์ (`actingAs` มีอยู่แล้วในโค้ด layout บรรทัด 48-49) หรือให้แม่บ้านตัวจริงลอง · **ยังไม่ได้หาว่าปุ่มสวมสิทธิ์อยู่ตรงไหน — CEO สั่งจบงานก่อน**
- ⚖️ **ทนายตรวจเนื้อหาสัญญา** — ทีมกฎหมายใน workshop เตือน 3 จุด (เรียก "ผู้รับจ้าง" แต่คุมแบบลูกจ้าง → พ.ร.บ.คุ้มครองแรงงาน ม.5 ดูพฤติการณ์จริง · ข้อ 3 ชดใช้เต็มจำนวนไม่มีเงื่อนไข → ป.พ.พ.ม.383 · บัตร ปชช. มีศาสนา → PDPA ม.26) CEO เลือกคงเนื้อหาเดิมไว้ก่อน → โค้ดออกแบบให้เนื้อหาสัญญาอยู่จุดเดียว แก้ทีหลังไฟล์เดียวจบ

**Spec เต็ม:** `docs/WORKSHOP_chairops-maid-contract-signing.md`

---

## 🪑📝 ChairOps — วางแผน "เซ็นสัญญาจ้างออนไลน์" สำหรับแม่บ้าน (2026-09-22 · WORKSHOP LOCKED — งานจริงทำแล้ว ดู entry ด้านบน)

CEO แจ้ง: อยากให้แม่บ้านเซ็นสัญญาจ้างออนไลน์ได้ (แนบเอกสาร+เซลฟี่ยืนยันตัวตน+เบอร์ฉุกเฉิน 2 คน+เลขบัญชี) ดีไซน์สไตล์เดียวกับสัญญาเช่าของ RentSpace

**รัน `/feature-workshop` (12-seat war-room) — Round 1 เต็ม, Round 2/3 ยุบเป็น synthesis เพราะ CEO ตอบคำถามที่แพงที่สุดเองกลางที่ประชุม:**

- สัญญาที่ใช้จริง = "สัญญาจ้างเหมาทำความสะอาดและเก็บเงินนำส่งธนาคาร" ฉบับที่ ChairOps มีอยู่แล้วในระบบ (ไม่ใช่ภาพสัญญาจ้างแรงงานทดลองงานที่ส่งมาผิดตอนแรก)
- **CEO ยืนยันกลางที่ประชุม (ก่อน R2 เริ่ม): ไม่ต้องสร้าง public/anonymous link ใหม่เลย** — แม่บ้านเก่าที่มี login อยู่แล้ว ส่งลิงก์เข้า session เดิมตรงๆ จากหน้าแม่บ้าน / พนักงานใหม่ใช้ลิงก์เชิญ LINE-login เดิม (`createMaidInvite`) แล้วต่อเข้าสัญญาทันที — ตัดงานที่แพงที่สุด (token wrapper+anonymous upload route ที่ Recruit workshop เมื่อ 09-20 ประเมินไว้ 14-16 วัน) ออกทั้งก้อน
- CEO ยืนยัน: **ใช้แค่ ChairOps พอ** ไม่ต้องทำร่วมกับ Recruit
- งานจริงที่เหลือ (เล็กลงมาก): ผูก onboarding form เดิม (`/m/onboarding`) เข้ากับหน้าเซ็นสัญญาเดิม (`/m/contract`) เป็น flow เดียว, เพิ่มเซลฟี่ (ยืม component จาก Playland, ต้องต่อสายให้เก็บรูปจริงเพราะของเดิมถ่ายแล้วทิ้ง), เพิ่มเบอร์ฉุกเฉินคนที่ 2 (migration), restyle ด้วย RentSpace's `.rs-scope` token
- ⚠️ **เจอความเสี่ยงกฎหมายที่ต้องแก้ก่อน ship**: สัญญาเรียกแม่บ้านว่า "ผู้รับจ้าง" แต่มีลักษณะควบคุมแบบลูกจ้าง (พ.ร.บ.คุ้มครองแรงงาน ม.5 ดูพฤติการณ์จริงไม่ใช่ชื่อสัญญา), บัตร ปชช.มีช่องศาสนา=ข้อมูลอ่อนไหวพิเศษ PDPA ม.26, ข้อ 3 "ชดใช้เต็มจำนวนไม่มีเงื่อนไข" เสี่ยงศาลเพิกถอน (ป.พ.พ.ม.383) — **รอทนายตรวจก่อน enable ให้แม่บ้านเซ็นจริง**

**Spec เต็ม:** `docs/WORKSHOP_chairops-maid-contract-signing.md` — มี Open Questions 4 ข้อรอ CEO ตอบ (ทนายตรวจสัญญา, เก็บเซลฟี่ถาวรไหม, ทำ v1 ครบทีเดียวหรือแบ่งเฟส, timeline)

**สถานะ:** ยังไม่เขียนโค้ด — รอ CEO ตอบ Open Questions ก่อน แนะนำเริ่มคู่ขนานเรื่องทนายตรวจสัญญาตั้งแต่วันนี้เพราะน่าจะใช้เวลานานกว่า dev เอง

---

## 🪑💰🚀 ChairOps "สลิป" แดงผิด + ยอดฝากเงินหายจากตัวเลขสาขา — แก้ครบทั้ง root cause + ย้อน FIN-03 + รวมชิปสลิป + ปุ่มตรวจแล้ว + แนบสลิปเพิ่ม (2026-09-22 · **DEPLOYED LIVE** `ac691411`)

CEO แจ้ง: พนักงานกดฝากเงินแนบสลิปแล้ว แต่ยอดไม่โผล่ในช่องฝากเงินของสาขา + คอลัมน์ "สลิป" ขึ้นแดงมีเครื่องหมายตกใจโดยไม่รู้สาเหตุ

**สาเหตุ (2 อย่างซ้อนกัน):**
1. บั๊กเทียบเลขบัญชีสลิปผิด (มีตัวแก้ทำไว้แล้วตั้งแต่ 09-20 แต่ยังไม่เคย deploy — ดู [[chairops-slip-account-mask-false-positive-2026-09-20]]) — ยังทำให้เกิดรายการใหม่ต่อเนื่องอยู่จนถึงวันนี้
2. FIN-03 (deploy ไปแล้วเมื่อ 09-20 ตามที่ CEO อนุมัติตอนนั้น) — ดึงยอดที่ติดธง "รอตรวจสอบ" ออกจากตัวเลขสาขาทันที **โดยไม่มีการแจ้งอะไรฝั่งพนักงานเลย** — พนักงานเห็นแค่ "บันทึกแล้ว ✓" ปกติ แต่ยอดสาขาไม่ขยับ

**ทำแล้ว:** deploy ตัวแก้บั๊ก #1 (`endsWith()`→`includes()`) — กันไม่ให้เกิดรายการผิดใหม่ต่อจากนี้

**ยังไม่จบ — ต้องมีคนกดต่อ:** deploy โค้ดใหม่ไม่ได้ล้างรายการเก่าที่ติดธงไปแล้วอัตโนมัติ (ค่านี้บันทึกไว้ตอนฝากครั้งเดียว ไม่คำนวณซ้ำ) — เช็คฐานข้อมูลจริงพบ **18 รายการยังติดธงอยู่ตอนนี้ (15 รายการคือ false-positive จากบั๊กนี้เป๊ะ, 3 รายการบัญชีต่างจริง)** — office ต้องกดปุ่ม "ตรวจแล้ว" ทีละรายการที่ `/chairops/review-queue` (ยังไม่มีปุ่ม "ตรวจทั้งหมดพร้อมกัน")

**✅ CEO ตัดสินใจแล้ว + deploy แล้ว (ต่อในเทิร์นเดียวกัน):** ยอด "เงินขาด" ของสาขาให้นับรวมทันทีเสมอไม่ต้องรอตรวจ (ย้อน FIN-03) + รายการฝากที่พนักงานเห็น (หน้า `/chairops/m/collect/[id]`) ให้โชว์ยอด+สีเขียว/แดงเสมอแทนการซ่อน — **DEPLOYED LIVE `8932a569`** (revert filter ใน `drift-engine.ts` ×2 + `_deposits.ts` ×1 + เพิ่ม badge สีในหน้าแม่บ้าน) ตามด้วย `c967e561` (แก้ comment ที่ค้างอ้างอิงพฤติกรรมเดิม) — ledger-push (ส่งเข้า LedgerLine) และ LINE แจ้งเตือน office ไม่แตะ เป็นคนละเรื่องกัน

**Verify:** `tsc --noEmit` 0 error · `eslint` 0 error · `next build` ผ่านทุก route ทั้ง 3 รอบ deploy · smoke `/`, `/login`, `/chairops`, `/chairops/reconcile/...` ตอบ 200/307 ปกติทุกครั้ง

---

**Part 2 (ต่อวันเดียวกัน) — CEO ดูหน้า Ledger จริง (สาขา "mpark" ไม่ใช่เคนซิงตัน) ถามเพิ่ม 4 ข้อ:**

1. ✅ "เก็บเงินมาได้เท่าไหร่" มีอยู่แล้ว — กดตัวเลขช่อง "ฝาก" เปิดดูรายละเอียดได้เลย ไม่ใช่บั๊ก
2. ✅ ยืนยันแล้ว ช่อง "ฝาก" คือยอดที่พนักงานพิมพ์กรอกเองจริง ไม่ใช่ระบบคำนวณแทน
3. ✅ **DEPLOYED LIVE `fb0d02e0`** — วันที่มีหลายสลิป เดิมขึ้นเป็นชิปแยกต้องบวกเลขเอง ตอนนี้รวมเป็นชิปเดียว (จำนวนใบ·ยอดรวม) กดขยายดูรายใบได้ — เฉพาะแท็บ Ledger เท่านั้น (แท็บ Periods ตั้งใจไม่แตะ เพราะ CEO เคยขอให้แยกชิปมาแล้วเมื่อ 08-17 ด้วยเหตุผลตรงข้ามกัน)
4. ⚠️→✅ **เจอบั๊ก cache ค้าง (mpark 16,850 vs ค่าจริง ~1,060) — เจอทางแก้ดีกว่ารอตี 5:** หน้า "ทุกสาขารวม" มีปุ่ม **"↻ Recompute" อยู่แล้ว** (ข้าง Export CSV) กดแล้วคำนวณใหม่ทั้ง 52 สาขาทันที ปลอดภัย ไม่ส่งแจ้งเตือนสแปม — บอก CEO ให้กดเองได้เลย ไม่ต้องรอผม/รอ cron

**Part 2.5 — CEO ถามว่า "ค้างฝาก" คืออะไรกันแน่:** ตอบแล้ว = (ยอดที่ POS นับว่าขายได้) − (ยอดฝากเข้าบัญชีจริง+ตัดยอดขาดที่อนุมัติแล้ว) — ตัวเลขนี้รวม "เก็บมาแล้วรอฝาก" (ปกติ) กับ "เงินหายจริง" (ต้องสอบ) ปนกัน ไม่แยก — ถ้าอยากดูแยกเฉพาะ "รอฝากยังไม่หาย" มีคอลัมน์ "เก็บ·ยังไม่ฝาก" แยกอยู่แล้ว

**Part 3 — CEO ขอ 3 ฟังก์ชันเพิ่มจากชิปสลิปแดงในหน้า Ledger:**
1. ✅ **DEPLOYED LIVE `d87047c6`** — กดชิปแดง เปิดดูรูปสลิปเต็มจอ มีปุ่ม "✓ ตรวจแล้ว ปกติ" ให้กดยืนยันตรงนั้นเลย ไม่ต้องไปหน้า review-queue แยก
2. 🟡 ปุ่มลบ (ต้องขออนุมัติ+เก็บ log) — สโคปแล้ว มีต้นแบบระบบอนุมัติจากระบบ "ตัดยอดขาด" ที่มีอยู่แล้วให้ก๊อปมาใช้ได้ แต่ deposit ยังไม่มีระบบ soft-delete (ต้องเพิ่ม migration) + ต้องแก้จุดคำนวณเงินหลายจุด (แบบเดียวกับ FIN-03) — รอ CEO ยืนยันว่าใครควรอนุมัติ (แนะนำ CEO เท่านั้น เข้มกว่า write-off เพราะลบรายการฝากกระทบมากกว่า)
3. ✅ **DEPLOYED LIVE `ac691411`** — CEO ยืนยัน: มีได้มากกว่า 1 สลิปจริง (เผื่อแม่บ้านลืมแนบ ส่งมาทาง LINE ทีหลัง) — สร้างตารางใหม่ `ChairopsDepositSlipAttachment` (migration apply เข้า prod จริงแล้ว verify ผ่าน check-schema-applied.mjs) กดปุ่ม "+ แนบสลิปเพิ่ม" ในป็อปอัพดูสลิปได้เลย ไม่ต้องขออนุมัติ ไม่กระทบยอดเงิน/สถานะตรวจสอบใดๆ — **ช่องโหว่ที่รู้ไว้ ยังไม่ครอบคลุม:** รายการที่นำเข้าจาก CSV ที่ไม่มีสลิปเลยตั้งแต่แรก (คนละจุด render กับที่แก้) ยังแนบสลิปแรกไม่ได้จากตรงนี้ รอบหน้าต้องทำเพิ่ม

**ค้าง — ข้อ 2 (ปุ่มลบ+ขออนุมัติ+log):** ยังไม่เริ่ม (CEO เลือกให้ทำข้อ 3 ก่อน) — สโคปไว้แล้ว: CEO อนุมัติเองเท่านั้น + reuse ระบบขออนุมัติจากตัดยอดขาด + ต้องเพิ่ม soft-delete ให้ ChairopsCashDeposit + แก้จุดคำนวณเงินหลายจุด

---

## 💵🐌🚀 CashHub หน้าแรกเปลี่ยนจาก "ภาพรวม" (ค้างโหลด) → "ศูนย์นำเข้าข้อมูล" + แก้ query กินข้อมูลไม่จำกัด (2026-09-20 · **DEPLOYED LIVE** `dba2af35`)

CEO แจ้ง: เปิด `/cashhub/dashboard` มาแล้วค้างเป็น skeleton โหลดไม่จบ ใช้จริงไม่ได้ และรู้สึกว่ามันกินข้อมูลตอนเปิดเฉยๆ ขอให้เปลี่ยนหน้าแรกไปที่ "ศูนย์นำเข้าข้อมูล" แทน

**พบสาเหตุ:** ต่อจาก [[pooilgroup-supabase-egress-quota-incident-2026-09-19]] Batch 2 ที่ค้างไว้ — query `branch_health_scores` ใน `lib/cashhub/aggregator.ts` ไม่มีขอบเขตวันที่/limit เลย ดึง "คะแนนสุขภาพสาขา" ย้อนหลังทั้งหมดตั้งแต่วันแรกทุกครั้งที่เปิดหน้า ทั้งที่ต้องการแค่ค่าล่าสุดต่อสาขา (ระบบเพิ่มแถวใหม่ 1 แถว/สาขา/คืน ไม่มีวันหยุด) — บั๊กคลาสเดียวกับที่เคยทำโควต้า Supabase ล็อกทั้งระบบ

**แก้แล้ว:**
1. `app/(admin)/cashhub/page.tsx` — หน้าแรก CashHub redirect จาก `/cashhub/dashboard` → `/cashhub/import` (ศูนย์นำเข้าข้อมูล) — CEO เลือก **เก็บ "ภาพรวม" ไว้ในเมนู** (ไม่ลบ) แค่ไม่ใช่หน้าแรกแล้ว
2. `lib/cashhub/aggregator.ts` — ใส่ขอบเขต 14 วัน + `.limit(2000)` กันโต ให้ query เดียวกับด้านบน (14 วันพอสำหรับคะแนนคำนวณรายคืน ไม่กระทบผลลัพธ์ที่ใช้แสดง)

**Verify:** `tsc --noEmit` 0 error · `eslint` 0 error · `next build` ผ่านทุก route (รวม `/cashhub`, `/cashhub/dashboard`, `/cashhub/import`) · `git status` สะอาด (แก้แค่ 2 ไฟล์ตามที่ตั้งใจ)

**สถานะ:** CEO อนุมัติ deploy 2026-09-21 → merge เข้า `setup` (`dba2af35`) push ผ่านตรงๆ (ไม่โดน classifier บล็อกรอบนี้) — Vercel ขึ้นเวอร์ชันใหม่แล้ว (dpl เปลี่ยนยืนยันแล้ว) — smoke `/`, `/login`, `/cashhub`, `/cashhub/dashboard`, `/cashhub/import` ตอบ 200/307 ปกติหมด ไม่มี 500 — **DEPLOYED LIVE** — post-mortem เขียนแล้ว (RULE O) `postmortems/cashhub-health-score-unbounded-query-2026-09-21.md` (commit `3bc9abed`, push ขึ้น `setup` แล้ว)

---

## 🔗⏳✅ ลิงก์เชิญพนักงานทุกโปรแกรม — มาตรฐาน 24 ชม. + เคลียร์ทิ้งอัตโนมัติ + แก้บั๊กเชิญซ้ำเงียบๆ + แก้ลิงก์เก่าไม่ล็อกอินให้ (2026-09-20 · BUILT+VERIFIED, COMMITTED ยังไม่ push/deploy)

CEO แจ้ง: ลิงก์เชิญพนักงานควรมีอายุ ~1 วันพอ ใช้เสร็จแล้วเคลียทิ้งได้เลย (เปลืองเปล่า) + แม่บ้านเก้าอี้นวดคนหนึ่งรับเชิญไปแล้วแต่ผูกสาขาไม่ติด login ไม่ได้

**พบว่า:** ระบบเชิญพนักงานไม่ได้ใช้ตัวเดียวกันทั้งบริษัท มี 4 จุดแยกกัน (ระบบกลาง+Recruit 48ชม., ClawFleet 7วัน, ChairOps 30วัน — CEO เคยสั่งขยายจาก 14 วันเมื่อ 18 มิ.ย. 69, LedgerLine 3 path บางจุดไม่มีวันหมดอายุเลย) และ LedgerLine ไม่เคยลบลิงก์ที่ใช้แล้วเลย

**แก้แล้ว (มาตรฐาน 24 ชม. ทุกจุด — CEO ยืนยันให้ทับคำสั่งเดิมของ ChairOps ที่เคยขยายเป็น 30 วัน):**
1. ระบบกลาง+Recruit (`lib/auth/invite.ts`), ClawFleet (`lib/clawfleet/team-actions.ts`), ChairOps (`lib/chairops/line/invite.ts`) — 48ชม./7วัน/30วัน → 24 ชม. ทั้งหมด + แก้ UI ที่บอกอายุลิงก์ผิดให้ตรง (เจอ ClawFleet UI ค้างเขียน "48 ชม." ทั้งที่โค้ดจริงเป็น 7 วันมานานแล้ว — stale-text บั๊กเดิมที่ไม่มีใครสังเกต)
2. LedgerLine (`app/(admin)/ledger/_actions.ts`) — 3 จุดสร้างลิงก์ (เชิญพนักงาน/self-claim/เชิญแอดมิน) ตั้ง default 1 วันหมด (เดิมจุดเชิญพนักงานทั่วไปไม่มีวันหมดอายุเลยถ้าไม่กรอก)
3. LedgerLine cleanup (`app/(admin)/ledger/_data.ts`) — CEO เลือก "เก็บร่องรอยเบาๆ" ไม่ใช่ลบทิ้งจริง (บัญชีต้องมี audit trail ว่าใครรับเชิญเมื่อไหร่) → หน้าจัดการลิงก์ไม่โชว์ลิงก์ที่ใช้แล้วอีกต่อไป (record ยังอยู่ใน DB) + ลิงก์ที่หมดอายุแล้วไม่เคยถูกใช้เลย (ไม่มีค่า audit) ลบทิ้งอัตโนมัติทุกครั้งที่เปิดหน้า ไม่ต้องตั้ง cron

**บั๊กแม่บ้านผูกสาขาไม่ติด:** เจอ 2 สาเหตุร่วม — (1) แม่บ้านน่าจะเข้าแอปโดยไม่ผ่านลิงก์เชิญ (เช่น กดไอคอน LINE ตรงๆ) → ระบบสร้างบัญชีไม่มีสาขาอัตโนมัติ (ของเดิมตั้งใจให้ทำแบบนี้ ซ่อมเองได้ถ้ากดลิงก์เชิญเดิมซ้ำ — ไม่แตะ) (2) **บั๊กจริงที่แก้แล้ว:** เชิญแม่บ้านคนที่ 2 เข้าสาขาเดียวกันจะไปยกเลิกลิงก์เชิญของคนแรกแบบเงียบๆ โดยไม่เตือนแอดมิน — เพิ่ม confirm dialog ก่อนยกเลิก (`app/(admin)/chairops/users/actions.ts` + ฟอร์ม 2 จุด) — CEO ต้องไปกดผูกสาขาให้แม่บ้านคนนี้เองที่ /chairops/maids (ปุ่มมีอยู่แล้ว ไม่ต้องรอ deploy)

**อัปเดต — CEO ถามตรงๆ "ลิงก์ที่ใช้ไปแล้ว ไม่ควรพาไปหน้า login เลยหรอ":** ถูกต้อง ควรเป็นแบบนั้น แต่โค้ดเดิมไม่ทำ — เจอบั๊กจริง: `app/api/auth/line-login/route.ts` เดิมพอลิงก์เชิญมีปัญหาอะไรก็ตาม (หมดอายุ/ถูกแทนที่ด้วยลิงก์ใหม่/คนอื่นใช้ไปแล้ว) จะ**ขึ้น error ค้างทันที** ไม่เคยลองเช็คว่า "คนนี้ผูกบัญชีไว้แล้วหรือยัง" ทั้งที่โค้ดจุดถัดไปมี logic เช็คแบบนั้นอยู่แล้วแค่ไม่เคยถูกเรียกใช้ — ดึง logic ผูกลิงก์เชิญออกมาเป็นฟังก์ชันแยก (`resolveChairopsInvite`) ให้ปัญหาลิงก์ทุกแบบ "ตกไปลองล็อกอินแบบปกติ" แทนที่จะค้าง error — คนที่ผูกบัญชีไว้แล้ว (ส่วนใหญ่) จะเข้าได้ทันทีแม้ลิงก์เชิญเก่าจะตายแล้ว ความปลอดภัยเดิมยังอยู่ครบ (LINE คนละคนแย่งบัญชีคนอื่นไม่ได้เหมือนเดิม)

**เช็คด้วยว่า "ทุกคนใช้ไม่ได้" จริงไหม:** ตรวจ deploy ล่าสุดที่ขึ้นเว็บจริงวันนี้ (`33bc54d6`) แล้ว — แก้แค่สิทธิ์ปิด/เปิดสาขาฝั่งออฟฟิศ ไม่ได้แตะระบบ login ของแม่บ้านเลย และเว็บตอบสนองปกติไม่ได้ล่ม → ไม่พบหลักฐานว่ามีการแก้โค้ดที่ทำให้แม่บ้านทุกคนพังพร้อมกัน รอ CEO ยืนยันจากแม่บ้านคนเก่าอีกทีว่ายังใช้ได้ปกติไหม

**Verify:** `tsc --noEmit` 0 error · `eslint` 0 error ในทุกไฟล์ที่แตะ

**สถานะ:** PUSHED + preview พร้อมแล้ว — branch `claude/invite-link-ttl-standardize-2026-09-20` (`c7efe4c2`, `43dcd589`) บน `origin`, preview: https://pooilgroup-git-claude-invite-link-ba0b85-patipan1122s-projects.vercel.app (Vercel build Ready, smoke 302 ทุกหน้าที่แตะ — ยังไม่ได้ทดสอบ flow LINE login จริงเพราะต้องใช้ LINE app จริงทดสอบ) — **ยังไม่ merge เข้า `setup`/deploy จริง** รอ CEO ทดสอบ preview ก่อน

---

## 🧾📅🚧 LedgerLine TRCloud reconcile — ปุ่มเลือกช่วงเดือน + สรุปยอด all-time กันตกหล่น (2026-09-20 · BUILT+VERIFIED, PUSHED branch แยก, ยังไม่ deploy)

ต่อจาก [[pooilgroup-supabase-egress-quota-incident-2026-09-19]] Batch 2 — หน้า `/ledger/trcloud-docs/reconcile` เดิมดึงประวัติ push+sync ทั้งหมดตลอดกาลทุกครั้งที่เปิดหน้า (ส่วนหนึ่งของสาเหตุ egress ล้น) ผู้เชี่ยวชาญเตือนว่าถ้าตัดดูแค่ล่าสุดตรงๆ ตัวเลขสรุป (ตรงกัน/ไม่ตรง/หาไม่เจอ) จะหดโดยไม่มีใครรู้ตัว (เช่น 3,400→45)

**ทำแล้ว:** เพิ่มปุ่มเลือกช่วงเดือน (reuse component เดิมจากหน้า bank-recon) — ตารางรายละเอียดเริ่มต้นโชว์แค่เดือนปัจจุบัน กดขยายดูย้อนหลังได้ ส่วนการ์ดสรุปด้านบน ("สรุปทั้งหมด — ทุกช่วงเวลา") คำนวณจากข้อมูลทั้งหมดเสมอ ไม่ขึ้นกับตัวกรองเดือน — ยืนยันด้วยตัวเองอีกชั้น (อ่านโค้ดจริง ไม่ใช่แค่เชื่อรายงาน) ว่า summary คำนวณก่อนกรองช่วงเดือนจริง เจอบั๊ก "select เกินจำเป็น" ที่คาดไว้กลับไม่มีอยู่แล้ว (ทำถูกไว้ตั้งแต่คอมมิตแรก)

**สถานะ:** branch `claude/ledger-trcloud-reconcile-monthrange` commit `53a791a5` — tsc/lint/build ผ่านหมด, มี preview link แล้ว, **ยังไม่ deploy** รอ CEO ตัดสินใจ (test preview ก่อน หรือ deploy เข้า production เลย)

---

## 🔑✅ CEO เข้า pooilgroup.com ไม่ได้ (วนกลับหน้า login) — ไม่ใช่บั๊ก เป็นจังหวะ deploy ชนพอดี (2026-09-20)

CEO แจ้งล็อกอินไม่ได้ วนกลับมาหน้า login ตลอด — ตรวจแล้วไม่ใช่ Supabase โดนล็อกเหมือนเมื่อวาน ([[pooilgroup-supabase-egress-quota-incident-2026-09-19]]) ทุกจุด (บัญชี CEO, ฐานข้อมูลตรง, Supabase Auth API) ปกติดีหมด และ log จริงแสดงว่า **login สำเร็จ 4 ครั้ง** ในช่วง 70 วินาที (12:19-12:20 น.) แต่หน้าเว็บไม่พาเข้าแดชบอร์ดสักครั้ง

**สาเหตุ:** ช่วงเวลาตรงกันเป๊ะกับตอนที่ deploy `33bc54d6` (แก้สิทธิ์ program_admin ด้านล่าง) กำลังขึ้นเว็บจริง (`dpl_DiWVT8mcaLkF5iuRgRYozeSyM43P`, ใช้เวลา ~4 นาที) — เบราว์เซอร์ CEO ค้างหน้า login เวอร์ชันเก่าไว้ตอนเว็บกำลังสลับเวอร์ชัน ทำให้ล็อกอินสำเร็จฝั่งเซิร์ฟเวอร์จริง แต่หน้าจอไม่ตามไปด้วย

**แก้:** ให้ CEO hard refresh (Cmd+Shift+R) แล้วลองใหม่ → **เข้าได้ปกติ ยืนยันแล้ว** ไม่ต้องแก้โค้ด — ตรวจ diff ของ `33bc54d6` ที่ deploy ไปแล้วด้วยตัวเองอีกชั้น พบว่าแก้เฉพาะสิทธิ์ role program_admin เท่านั้น ไม่แตะ session/login ของ super_admin เลย

**บทเรียน:** เจอ "login วน" อีก → เช็คเวลา deploy ล่าสุดก่อน (ตรงกับตอนผู้ใช้พยายาม login ไหม) ก่อนขุดโค้ด auth ลึก — ดู [[feedback-login-loop-during-deploy-cutover-2026-09-20]]

---

## 🔐🚧 CashHub program_admin สิทธิ์ไม่ครบ (บั๊กคลาสเดิม รอบ 4) + อัปเกรดตัวกันพลาดทั้งเรโป (2026-09-19 · BUILT+VERIFIED ใน worktree, ยังไม่ push ขึ้น setup)

ไฮ (program_admin, 14/16 โปรแกรม) แจ้งว่า CashHub ไม่มีปุ่ม "ส่งเข้าบัญชี LedgerLine" และตั้งค่าเลขบัญชีไม่ได้ — บั๊กคลาสเดียวกับที่แก้มาแล้ว 3 รอบก่อนหน้า (`c94c8c17` 17 ส.ค. → `fc05c59b` 6-7 ก.ย. → `603ab804` 9 ก.ย.) CEO สั่งให้แก้ถาวรทั่วทั้งเรโป และเลือกทางเลือก "รื้อสถาปัตยกรรมทั้งระบบ (program_admin ได้ทุกอย่างยกเว้นที่ห้ามชัดเจน)" แทนทางเลือกต่อยอดของเดิม — ทำเป็น 2 ก้อนเพื่อความปลอดภัย เพราะไล่แก้ทีเดียวทั่ว ~400 จุดเสี่ยงเกิน (บางจุดเป็น org-wide platform route ที่ถ้าเปิดให้ program_admin จะกลายเป็น privilege escalation)

**สาเหตุ CashHub:** ปุ่ม/หน้าตั้งค่าทั้ง 4 ช่องทาง (Amazon/ชา/โรงแรม/ปั๊มน้ำมัน) ล็อกด้วย `isSuperAdmin()`/`requireSuperAdmin()` เขียนไว้ 14 มิ.ย. 2569 — **ก่อน** concept program_admin จะเกิดขึ้นด้วยซ้ำ (16 มิ.ย.) ตัวกันพลาดอัตโนมัติ (CI guardrail) จาก 2 รอบก่อนมองไม่เห็นเพราะสแกนหาแค่แพทเทิร์น "รายชื่อ role" ไม่ใช่ "ฟังก์ชันเช็คเดี่ยว"

**แก้แล้ว:**
1. CashHub Amazon/Tea/Hotel/ปั๊มน้ำมัน — ปุ่มส่ง reconcile + หน้าตั้งค่าบัญชี ทั้งหน้าจอ+API หลังบ้าน (`isSuperAdmin` → `isProgramAdminTier`) รวม 4 จุด API ที่ลิสต์ตรวจสอบรอบแรกพลาดไป (amazon-settlement/save, tea/channel-config, tea/match-rule, hotel-settlement/save)
2. [`LedgerBottomNav.tsx`](components/ledger/LedgerBottomNav.tsx) เมนูมือถือ — เพิ่ม program_admin ใน FINANCIAL/BUDGET array (เดิมเห็นแค่แท็บ "ตั้งค่า" แท็บเดียว)
3. [`cashhub/reports/route.ts:109`](app/api/cashhub/reports/route.ts#L109) เช็คข้ามสาขา — แก้ array ตามที่ CEO อนุมัติแล้ว **แต่ยังไม่มีผลจริง** เพราะ `lib/auth/permissions.ts` MATRIX.program_admin ไม่มี `cashhub.create` เลย ถูก 403 ก่อนถึงจุดนี้ตั้งแต่ line 64 — ต้องคุยแยกว่าจะแก้ permissions.ts ด้วยไหม (ขัดกับ `canFillReports()` ที่ตั้งใจกันไว้เดิม)
4. **ขยาย CI guardrail** ([`role-gate-completeness.cases.ts`](lib/auth/__tests__/role-gate-completeness.cases.ts)) ให้จับ `isSuperAdmin`/`requireSuperAdmin`/`isAdminTier`/`requireAdminTier` แบบเดี่ยวด้วย (ของเดิมจับแค่ array) — รันทั่วเรโป 254 จุด เจอเพิ่ม 12 จุดที่เป็นบั๊กแบบเดียวกันจริง (RentSpace floor-plan/payments, LedgerLine web+LIFF+LINE-bot) แก้ให้แล้วโดยเทียบกับโค้ดพี่น้องที่ถูกอยู่แล้วก่อนแก้ทุกจุด ที่เหลือ ~40 จุดใส่ไว้ใน [`role-gate-known-exceptions.ts`](lib/auth/role-gate-known-exceptions.ts) พร้อมเหตุผล รอ CEO ตัดสินใจทีละจุด (ส่วนใหญ่เป็นอนุมัติวงเงิน/ลบข้อมูล/รหัสลับเชื่อมต่อภายนอก)

**Verify:** `prisma generate` → `tsc --noEmit` 0 error → eslint 0 error ใหม่ → guardrail 254/254 ผ่าน → `next build` 693 route สำเร็จ → ทดลอง revert 1 จุดแล้วยืนยัน guardrail จับได้จริงที่ file:line ถูกต้อง (ทำ 2 รอบ)

**ค้าง (ก่อน):** (1) จุดที่ 3 (cashhub/reports ↔ permissions.ts) รอ CEO ตัดสินใจแยกต่างหาก (2) ~40 จุดใน exceptions file รอทยอยตัดสินใจทีละโปรแกรม (3) ยัง**ไม่ push ขึ้น `setup`**

---

**อัปเดต 2026-09-19 (วันเดียวกัน) — CEO ตัดสินใจครบทุกจุดค้าง แก้เสร็จแล้ว รอบ 2:**

1. **CashHub reports เปิดเต็มที่** — เจอว่ามี "ด่าน 2 ชั้น" (`lib/auth/permissions.ts` MATRIX.program_admin เดิมว่างเปล่า บล็อกก่อนถึงจุดที่แก้รอบแรกเสียอีก) CEO เลือก "เปิดให้กรอกได้เต็มที่" → เพิ่ม `cashhub.view`+`cashhub.create` ให้ program_admin ใน permissions.ts + แก้ [`canFillReports()`/`hasCrossBranchAccess()`](lib/auth/branch-access.ts) ให้รวม program_admin ด้วย — ตรวจ code path ครบยืนยันไม่มีด่านที่ 3 ซ่อนอยู่อีก (ยังไม่เปิดสิทธิ์อนุมัติ/ปลดล็อก/export ให้ — เฉพาะกรอกรายงานเท่านั้นตามที่อนุมัติ)
2. **~35 จุด "ล็อกไว้ถูกต้องอยู่แล้ว"** — CEO อนุมัติคงตามเดิมทั้งหมด (รหัสลับเชื่อมต่อ/ลบข้อมูลถาวร/กันอนุมัติงานตัวเอง) → ปิดสถานะ "รอตัดสินใจ" ในเอกสารเป็น "CEO ยืนยันแล้ว" ไม่แก้โค้ด
3. **4 จุดที่น่าสงสัยว่าอาจเป็นบั๊ก** — CEO ตัดสินทีละจุด: เปิดให้ program_admin ที่ Recruit (จัดการช่องทางรับสมัคร), RentSpace (นำเข้า Excel จำนวนมาก), ChairOps (ปิด/เปิดสาขารายตัว — คนละปุ่มกับปิดงวดทั้งองค์กรซึ่งยังคงล็อกไว้) · คงล็อกไว้ที่ Inbox (ผูกกลุ่ม LINE เข้าสาขา)
4. **DC (คลังกลาง) พ้นช่วงทดลองแล้ว** — CEO ยืนยันเปิดสิทธิ์ลบ/ย้อนเอกสารคลังทั้งชุด (7 หน้า + ฟังก์ชันหลังบ้าน 2 จุด) ให้ program_admin — คงล็อกเฉพาะหน้าเชื่อมต่อ Google Drive ของ DC ไว้เหมือนเดิม (คนละเรื่องกัน)

**Verify รอบ 2:** `prisma generate` → `tsc` 0 error → eslint 0 error ใหม่ → guardrail ผ่าน 243/243 (registry ลดจาก 154→142 จุด เพราะหลายจุดเปิดแล้วไม่ต้องมี exception อีกต่อไป) → `next build` ผ่าน → ทดลอง revert แล้วยืนยัน guardrail จับได้จริงอีกรอบ — **ตรวจ diff จริงเทียบกับที่อนุมัติไว้ด้วยตัวเองอีกชั้น** (ระบบ security-check ของ agent ขึ้นเตือนเพราะพรอมต์สรุปคำอนุมัติเป็นคำพูดตัวเองแทนที่จะ quote ตรงๆ — ตรวจแล้วโค้ดตรงกับที่อนุมัติ 100% เป็น false-positive แต่บันทึกเป็นบทเรียนไว้แล้ว)

**สถานะล่าสุด:** ทุกจุดที่ค้างตัดสินใจปิดครบแล้ว — branch `claude/program-admin-cashhub-full-fix-2026-09-19` commit `4e89942d` (ต่อจาก `5e01d430`) อยู่บน `origin` เฉยๆ **ยังไม่ push เข้า `setup`** รอสั่ง deploy

---

**🚀 DEPLOYED LIVE (2026-09-20)** — CEO สั่ง "deploy" → rebase บน `origin/setup` ล่าสุด (มี 3 commit ใหม่จาก session อื่นแทรกมาระหว่างนี้ — Recruit share-link + Supabase egress fix — เช็ค `git diff --stat` แล้วไม่มีไฟล์ชนกันเลย, safe rebase) → commit ใหม่ `33bc54d6` → รัน `/verify` ครบ 5 ด่าน (tsc 0 error · eslint 1 error พบแต่ยืนยันแล้วว่าเป็นของเดิมมีอยู่ก่อนแล้วใน production ไม่เกี่ยวกับงานนี้ · build ผ่าน · ไม่มีไฟล์ค้าง · smoke 10 route ก่อน deploy) → stamp + push เข้า `setup` (`58cd0ca4..33bc54d6`) → Vercel deploy `dpl_DiWVT8mcaLkF5iuRgRYozeSyM43P` Ready (~4 นาที) → `vercel inspect pooilgroup.com` ยืนยัน `pooilgroup.com`+`www.pooilgroup.com` ชี้เข้า deploy ใหม่แล้ว → smoke 10 route หลัง deploy ตรงกับก่อน deploy เป๊ะทุกจุด

ดู memory [[program-admin-cashhub-round4-and-guardrail-upgrade-2026-09-19]]

---

## 🧑‍💼🔗✅ Recruit — ลิงก์แชร์ดูผู้สมัคร ต่อประกาศเดียว (2026-09-19 · **DEPLOYED LIVE** `098f7ef8`)

CEO ถามหาลิงก์ส่งให้คนในทีมดูรายชื่อผู้สมัครของประกาศเดียวได้ — เช็คโค้ดแล้วไม่มีของเดิม (ปุ่ม "ดู N ใบ" เดิมต้องล็อกอินเป็นแอดมิน Recruit เท่านั้น, ไม่มี export CSV/PDF) เป็นฟีเจอร์ใหม่ CEO เลือกขอบเขตต่อประกาศเดียว (ไม่ใช่ทั้งโปรแกรม) — ตอนแรกเลือก "ต้องล็อกอินก่อนดู" แล้วเปลี่ยนใจภายหลังเป็น **"เปิดดูได้เลย ไม่ต้องล็อกอิน"** (เหมือนลิงก์ดูบิล RentSpace ที่มีอยู่แล้ว — รหัสลิงก์สุ่มยาวคือกุญแจในตัวเอง)

**สร้างแล้ว:**
- คอลัมน์ token ใหม่ต่อประกาศ (`applicant_share_token`, unique, สุ่มไม่ซ้ำ)
- ปุ่ม "แชร์ดูผู้สมัคร" หน้ารายการประกาศ — สร้าง/คัดลอก/ยกเลิกลิงก์ได้ (ยกเลิกแล้วลิงก์เก่าเปิดไม่ได้ทันที)
- หน้าดูอย่างเดียวที่ `/recruit-share/[token]` (นอกโซน `(admin)` ตั้งใจ — เปิดสาธารณะจริง ไม่ผ่านด่านล็อกอินใดๆ) เห็นชื่อ-เบอร์-ไฟล์แนบ-สถานะ-วันที่สมัคร ต่อประกาศนั้น **ไม่โชว์คะแนน/สรุป AI ภายใน** (ข้อมูลประเมินของแอดมินสงวนไว้)

**Verify:** tsc 0 errors, eslint 0 errors ใหม่, `next build` สำเร็จ (route `/recruit-share/[token]` ขึ้นถูกต้องเป็น public route) · migration apply เข้า DB จริงแล้ว ยืนยันด้วย `check-schema-applied` (`DB schema is up to date`) · push ตรงเข้า `setup` (`0f08890a..098f7ef8`, fast-forward ไม่มี conflict)

**ข้อควรรู้:** เพราะเปลี่ยนเป็น public ไม่ล็อกอิน — ใครก็ตามที่ได้ลิงก์ (ต่อ/ส่งต่อ/หลุด) เห็นข้อมูลผู้สมัครได้ทันที ไม่มีการยืนยันตัวตนคนเปิด — กดยกเลิกลิงก์ได้ตลอดเวลาจากหน้ารายการประกาศถ้าต้องการปิด

---

## 📦 STATUS.md housekeeping — auto-archive ตาม RULE M (2026-09-19)

ไฟล์เกิน 150,000 bytes (429KB) ก่อน append entry นี้ → ย้าย entry เก่ากว่า 2026-08-23 (ช่วง 2026-05-04→08-23, 331KB) ไปที่ `STATUS-archive.md` ใหม่ (ไฟล์แรกที่สร้างจริงตาม RULE M — เดิมยังไม่เคยมี) เหลือ STATUS.md ~91KB · เนื้อหาไม่หายไปไหน ย้ายเก็บครบ

---

## 🔐🧾✅ LedgerLine LIFF — ปุ่มขอสิทธิ์เข้าถึงสาขา + แก้บั๊ก "ขอโอน" เช็คข้อมูลเก่า (2026-09-19 · DEPLOYED LIVE)

**ปัญหาที่ CEO แจ้ง (จากสกรีนชอต):**
1. พนักงานที่ไม่มีสิทธิ์สาขานั้นแต่ยังเห็นบิลของสาขานั้นได้ — อยากให้มีปุ่มขอสิทธิ์อยู่ในแถบเตือนเดียวกัน เลือกจากสาขาทั้งหมด ให้ซุปเปอร์แอดมินอนุมัติ
2. พนักงานเลือกหมวดหมู่+สาขาแล้ว กด "ขอโอน" ระบบดันบอกว่ายังไม่เลือก (เพราะค่าที่เลือกยังไม่ถูกบันทึก — ระบบเช็คจากข้อมูลตอนโหลดหน้า ไม่ใช่ค่าที่เพิ่งเลือก)

**แก้ยังไง:**
- ปุ่ม "ขอสิทธิ์เข้าถึงสาขานี้" บนหน้า LIFF expense (`BranchAccessBanner.tsx` ใหม่) — โชว์ทันทีที่เปิดหน้าถ้าไม่มีสิทธิ์ (proactive ไม่ต้องรอกดพังก่อนเหมือนเดิม) — reuse ระบบเดิมทั้งหมด (`pendingBranchId` + ปุ่มอนุมัติ/ปฏิเสธที่มีอยู่แล้วในหน้าตั้งค่า, mirror คำสั่งไลน์ `/สาขา <ชื่อ>` เดิม) — เช็คแล้วว่า super_admin อนุมัติได้แน่นอน (bypass สิทธิ์ทุก module อัตโนมัติ) — เก็บคำขอได้ทีละ 1 คำขอต่อคน (ข้อจำกัดเดิมของระบบ, CEO เลือกให้ใช้แบบนี้ไปก่อนแทนสร้างตารางคิวใหม่)
- แก้ "ขอโอน" ให้เช็คสาขา/หมวดจากค่าที่จอโชว์จริง (ไม่ใช่ค่าตอนโหลดหน้า) + บันทึกอัตโนมัติก่อนเช็คเสมอ (เดิมบันทึกอัตโนมัติเฉพาะบางเคส) + ถ้ายังไม่เลือกจริง ๆ เด้ง popup บังคับเลื่อนไปเลือกก่อน แทนเปิดหน้าต่างขอโอนที่กดอะไรไม่ได้เฉย ๆ

**ไฟล์ที่แตะ:** `app/(admin)/ledger/_actions.ts`, `lib/ledger/liff-auth.ts`, `components/ledger/ExpenseReviewPane.tsx`, `app/liff/ledger/expense/[id]/{page,LiffExpensePane,LiffPayeeRequest}.tsx`, `app/liff/ledger/expense/[id]/BranchAccessBanner.tsx` (ใหม่)

**สถานะ:** typecheck สะอาด · lint ไม่มี error ใหม่ (4 error ที่เจอเป็นของเดิมในไฟล์อยู่แล้วก่อนแตะ ไม่เกี่ยวกับงานนี้) · `next build` ผ่านสะอาด · CEO อนุมัติ merge `c8f46cb2` เข้า `setup` แล้ว — **DEPLOYED LIVE** (Vercel `● Ready`, build 2m, smoke `/login` `/liff/ledger` → 200) — รอ CEO ทดสอบกดปุ่มจริงบนมือถือ 2 เคส: (1) เปิดบิลสาขาที่ไม่มีสิทธิ์ (2) เลือกหมวด/สาขาแล้วกด "ขอโอน" ทันทีไม่กดบันทึกก่อน

**⚠️ บทเรียนระหว่างทาง:** งานรอบแรก (13 ก.ย.) เขียนเสร็จ+verify ผ่านแล้วแต่ยังไม่ทัน commit — worktree ค้างไว้ 6 วันแล้วหายไปเงียบ ๆ (หลุดจาก `git worktree list`, ไม่มี `.git` เหลือ) ต้องเขียนใหม่ทั้งหมด — เก็บเป็น feedback memory แล้ว: ต่อไปนี้ commit ทันทีหลัง verify ผ่าน ไม่รอ push ตอนจบงาน

## 👥🔧✅ LINE login ไม่เคยบันทึกเวลาเข้าใช้ — แก้รากเสร็จแล้ว (2026-09-13 · DEPLOYED LIVE ครบทั้ง 2 ระดับ)

**ระดับ 2 (CEO อนุมัติ "แก้ราก เลย"):** [`app/api/auth/line-login/route.ts`](app/api/auth/line-login/route.ts) เพิ่มเรียก `recordSuccessfulLogin()` — ฟังก์ชันเดียวกับที่ login ด้วยอีเมล/รหัสผ่านใช้อยู่แล้ว (ผ่าน `/api/auth/post-login`) — ตรงจุดที่ resolve ผู้ใช้ + สร้าง magic link สำเร็จแล้ว (จุดเดียวกับที่ audit log LOGIN เดิมอยู่ ครอบคลุมทั้งเส้นทาง LIFF-JS ตรง และเส้นทาง OAuth ผ่าน `/auth/line-callback`) ตอนนี้ล็อกอินผ่าน LINE จะอัปเดต `last_login_at` เหมือนอีเมลแล้ว — แก้ทั้งหน้า "ทีม & สาขา" และ Access Review cron ที่เคยธงเท็จคนใช้ LINE เป็นหลัก

ไม่เช็ค/throw ผลลัพธ์จากฟังก์ชันนี้ (ถ้าล้มเหลวต้องไม่บล็อกการล็อกอินจริง — ตามแนวเดียวกับ audit() เดิมในไฟล์นี้)

**Verify:** `tsc --noEmit` 0 error · eslint เฉพาะไฟล์ที่แก้ 0 error · `next build` ผ่าน · smoke ก่อน/หลัง deploy ตรงกันเป๊ะ · **หมายเหตุ:** จุดนี้ทดสอบ end-to-end จริงไม่ได้ (ต้องมีคนล็อกอินผ่าน LINE จริง) — ยืนยันด้วยการอ่านโค้ด+ผลลัพธ์ build เท่านั้น แนะนำให้เช็คที่ `/users` หลังพนักงานคนไหนล็อกอินผ่าน LINE ครั้งถัดไป ว่า "LOGIN ล่าสุด" ขยับจริง

**🚀 DEPLOYED LIVE:** push `2e33bca6..08b0fc4b` เข้า `setup` → Vercel deploy Ready → domain ยืนยันชี้ deploy ใหม่ + smoke ตรงกันเป๊ะ

---

## 👥🕐✅ หน้าทีม & สาขา — "LOGIN ล่าสุด" ไม่แม่น + พบบั๊กราก LINE login ไม่บันทึกเวลาเข้าใช้ (2026-09-13 · DEPLOYED LIVE ระดับ 1, ค้างระดับ 2 — ปัจจุบันแก้ครบแล้ว ดูด้านบน)

CEO ดูหน้า `/users` (ทีม & สาขา) เห็นคอลัมน์ "LOGIN ล่าสุด" ขึ้น "31 วัน" / "96 วัน" / "—" กับพนักงานหลายคน สงสัยว่า "น่าจะไม่จริง" อยากให้โชว์วันที่/เวลาจริงแทน

**สาเหตุที่เจอ (ใหญ่กว่าที่คิด — ไม่ใช่แค่การแสดงผล):** ระบบมี 2 ช่องทางล็อกอิน — อีเมล+รหัสผ่าน (บันทึกเวลาล่าสุดถูกต้อง ผ่าน `recordSuccessfulLogin()`) กับ **LINE (ทางที่พนักงานหน้างาน/สาขาใช้เป็นหลัก) ซึ่งไม่เคยบันทึกเวลาล็อกอินล่าสุดเลยตั้งแต่ต้น** — พนักงานที่เข้างานผ่าน LINE ทุกวันเลยโชว์ค่าเก่า/ว่างทั้งที่ใช้งานจริง

**ผลกระทบ (3 จุด ใช้ค่าเดียวกัน):**
1. หน้า "ทีม & สาขา" — แสดงผิด (แก้แล้ว ดูด้านล่าง)
2. หน้าตั้งค่าทีม ClawFleet — แสดงผิดแบบเดียวกัน (ยังไม่แก้ — มี fallback โชว์วันที่หลังเกิน 7 วันอยู่แล้ว เสี่ยงน้อยกว่า)
3. **ระบบ Access Review อัตโนมัติ (แจ้งเตือน Telegram)** — ใช้ค่านี้ตัดสิน "ไม่ได้ใช้งานเกิน 45 วัน = ควรเพิกถอนสิทธิ์" **พนักงานที่ใช้ LINE เป็นหลักอาจโดนธงเท็จว่าไม่ได้ใช้งาน** — จุดนี้สำคัญสุด รอ CEO ตัดสินใจว่าจะแก้ระดับรากไหม

**FIX ระดับ 1 (เสร็จแล้ว) — เปลี่ยนการแสดงผล:** [`users-table-view.tsx`](<app/(admin)/users/users-table-view.tsx>) เปลี่ยนคอลัมน์จาก "N วัน" (ฟังก์ชัน `timeAgo` ที่ลบทิ้งแล้ว) เป็นวันที่+เวลาจริง (`bkkDateTime()` ฟังก์ชันเดิมที่มีอยู่แล้ว ใช้ในหน้ารายละเอียดผู้ใช้) — ไม่แตะระบบ login/auth เลย เสี่ยงต่ำ

**ค้าง (รอ CEO อนุมัติ) — ระดับ 2 แก้ที่ราก:** ทำให้ตอนล็อกอินผ่าน LINE บันทึกเวลาล่าสุดด้วยเหมือนอีเมล — ถ้าไม่แก้ ตัวเลขจะยังผิดอยู่ดีสำหรับคนที่ใช้ LINE เป็นหลัก (คนส่วนใหญ่) แม้จะแสดงผลเป็นวันที่จริงแล้วก็ตาม แตะระบบ auth ต้องระวังกว่าระดับ 1

**Verify:** `tsc --noEmit` 0 error · eslint เฉพาะไฟล์ที่แก้ 0 error · `next build` ผ่าน · smoke ก่อน/หลัง deploy ตรงกันเป๊ะ (`/`, `/login`, `/users` → 200 ทั้งคู่) · `vercel inspect pooilgroup.com` ยืนยัน domain ชี้เข้า deploy ใหม่แล้ว

**🚀 DEPLOYED LIVE (2026-09-13):** push `241a224c..9f364499` เข้า `setup` → Vercel deploy Ready (~3 นาที) → ยืนยัน domain ชี้ deploy ใหม่ + smoke ตรงกันเป๊ะ

**เพิ่มเติมวันเดียวกัน — เรียงตารางให้คนใช้งานอยู่บน:** CEO ขอต่อว่าอยากให้ตาราง "ทีม & สาขา" เรียงคนที่ใช้งานอยู่ด้านบน คนไม่ใช้ไปอยู่ล่าง — [`page.tsx`](<app/(admin)/users/page.tsx>) เรียง `flatUsers` (array ที่ป้อนตารางแบบ Excel เท่านั้น) ตาม `last_login_at` ใหม่→เก่า โดยคนไม่เคยล็อกอินเลยจมไปอยู่ล่างสุด — ไม่แตะ query ต้นทาง ไม่กระทบมุมมองการ์ด (ประเภทธุรกิจ→สาขา→คน) ซึ่งยังเรียง `created_at` เดิม. Verify: tsc/eslint/build ผ่านหมด · smoke ก่อน/หลัง deploy ตรงกันเป๊ะ. **🚀 DEPLOYED LIVE:** push `14c41cb1..61bb7d65` → Vercel Ready → domain ยืนยันชี้ deploy ใหม่

ดู memory [[users-lastlogin-display-and-line-login-not-tracked-2026-09-13]]

---

> 📦 ประวัติเต็มทุกรอบ (2026-05-04 → 2026-09-09) → ดู STATUS-archive.md

## 🎯 Where we are

**Sprint 0–2 ส่วนใหญ่เสร็จแล้ว + Sprint 3 (CashHub) อยู่ในมือ**
มี 11 commits, codebase พร้อม dev. ติดที่ยังไม่มี LINE/Telegram bot integration และ 7 forms ยังไม่ครบ.

**Stack ที่ใช้จริง:**
- Next.js **16.2.4** + React 19 + TS strict + Tailwind v4
- Prisma **7.8** + Supabase (Postgres + Auth + RLS) + Cloudflare R2
- shadcn-style UI + sonner (toast) + react-hook-form + zod
- ⚠️ Next.js 16 มี breaking changes — อ่าน `node_modules/next/dist/docs/` ก่อนเขียน

---

## ✅ Done (committed)

### Foundation
- [x] Next.js 16 + TS strict + Tailwind v4 init
- [x] Prisma schema **8 tables**: Organization, User, Branch, UserBranch, ReportTemplate, DailyReport, CashShortage, AuditLog
- [x] Supabase RLS migration (`supabase/migrations/20260504000001_rls_and_jwt_claim.sql`)
- [x] Supabase SSR client + middleware proxy
- [x] Cloudflare R2 client + signed-URL upload + uploader UI
- [x] Permission Matrix (6 roles, hardcoded — `lib/auth/permissions.ts`)
- [x] Session helpers (`lib/auth/session.ts`)
- [x] Audit log helper (`lib/audit/log.ts`)
- [x] Seed script + form configs

### Auth flow
- [x] Login / Signup / Forgot-password pages
- [x] **Invite token** flow (token → set password → first-user-becomes-Owner)
- [x] `/api/auth/signup`, `/api/auth/invite/accept`
- [x] Profile page + change password
- [x] 403 page

### Admin (web)
- [x] Admin shell (sidebar + navbar)
- [x] Pages: home, profile, settings, audit, users (+ new), docuflow placeholder, fuelos placeholder
- [x] `/api/admin/users` CRUD
- [x] R2 upload demo page

### CashHub MVP
- [x] **Universal ReportForm** engine (อ่าน `constants/business-types.ts` → render fields)
- [x] `constants/business-types.ts` (465 บรรทัด — 7 ประเภทธุรกิจครบ)
- [x] Reconcile indicator (real-time)
- [x] Shortage modal (เงินขาด → ระบุคน/หมายเหตุ)
- [x] Reconcile logic (`lib/cashhub/reconcile.ts`)
- [x] LIFF report page `/liff/report/[branchId]` (ใช้ session-based, ยังไม่ใช่ LIFF init จริง)
- [x] LIFF status page
- [x] `/api/cashhub/reports`, `/api/cashhub/approve`, `/api/cashhub/export`
- [x] Draft auto-save ใน localStorage

### DevOps
- [x] `.env.example` ครบ (DB / Supabase / R2 / LINE / Telegram / App)
- [x] R2 CORS script
- [x] Vercel deploy prep
- [x] Git: 11 commits, ประวัติสะอาด

---

## 🟡 Uncommitted (กำลังรีแสตรัคเจอร์)

```
Working tree changes:
 D app/(admin)/branches/page.tsx        ← ย้ายเข้า cashhub/branches/
 D app/(admin)/cashhub/page.tsx
 D app/(admin)/dashboard/*              ← ย้ายเข้า cashhub/dashboard/
 D app/(admin)/reports/*                ← ย้ายเข้า cashhub/reports/
 M app/(admin)/settings/page.tsx
 M app/(admin)/users/page.tsx
 M app/page.tsx
 M components/layout/admin-shell.tsx
 M components/ui/card.tsx

?? app/(admin)/cashhub/{branches,dashboard,reports}/   ← โครงสร้างใหม่
?? app/(admin)/{docuflow,fuelos,home}/
?? components/ui/{data-table,empty-state,section,stat-block}.tsx
?? lib/modules.ts
```

**= module-based folder restructure** ตามสเปค `(admin)/<module>/<page>` — ยังไม่ commit

---

## ⬜ Not started yet

### Sprint 0 ที่เหลือ
- [ ] **LINE Messaging API webhook** (`/api/line/webhook`)
- [ ] **LIFF init จริง** — `@line/liff` install แล้ว แต่ page ยังใช้ `requireSession()` แทน `liff.init()`
- [ ] **Telegram Bot** (Grammy, ยังไม่ install + ไม่มี `/api/telegram/webhook`)
- [x] ~~LINE Rich Menu config + upload script~~ — `scripts/line-rich-menu.mjs` + `npm run line:rich-menu`
- [ ] Telegram Admin Chat ID setup

### Sprint 1–2 ที่เหลือ
- [x] ~~Self-Register flow (`/join` page) + อนุมัติ~~ — admin queue (Telegram notify ค่อยทำกับ bot)
- [ ] Permission Templates UI (4 preset)
- [ ] Branch Groups (จัดกลุ่มสาขา) — มี table แล้ว, เหลือ UI
- [x] ~~Module Toggle UI per Org~~ — เพิ่ม per-user module access ด้วย
- [ ] Smart Digest (กัน Telegram spam) — รอ Telegram bot
- [x] ~~My Action Center widget~~
- [x] ~~Scheduled PDF Monthly Report~~ — Vercel cron `monthly-report-pdf`

### CashHub (Sprint 3–5)
- [ ] **ทดสอบ ReportForm 7 ประเภทครบ** (มี config แล้ว แต่ยังไม่ verify ครบทุก type)
- [x] ~~Spike Alert~~ (commit `692bfbb`)
- [ ] Anti-Stupidity ที่เหลือ: Time Alert (00:00–05:00), Pre-check Rule 7
- [ ] Approval ผ่าน **Telegram Inline** [✅][❌][📊] — Quick Approve Bar (web) มีแล้ว
- [x] ~~Smart Approval Panel (Web)~~ — Quick Approve Bar
- [x] ~~Analytics: Branch View + Super View + Calendar Heatmap~~
- [x] ~~Health Score A–F~~ + Cron (Vercel `health-score` 23:00 BKK)
- [x] ~~Branch Leaderboard + Streak Badge~~
- [x] ~~Drill-down: ภาพรวม → ธุรกิจ → สาขา → รายวัน~~
- [ ] AI Chat "Ask Me Anything" (Claude Haiku)
- [x] ~~Forecast สิ้นเดือน, Pattern Heatmap~~
- [ ] Quick Note Staff → เจ้าของ
- [x] ~~Missing Report Reason flow~~

### FuelOS (Sprint 6–7) — Sprint 6.0 schema ✅ (2026-05-11)
- [x] **Sprint 6.0 — Schema foundation** (16 models + RLS + GENERATED columns)
- [ ] Sprint 6.1 — Price Engine (depot price entry + zone margin admin) ← **next**
- [ ] Sprint 6.2 — CRM Multi-Entity (contacts/entities/locations + credit fields)
- [ ] Sprint 6.3 — Sales Workspace (Priority List + Quote/Win-Loss + Margin Analytics)
- [ ] Sprint 6.4 — LINE Bot (Reply API + Response Time tracking)
- [ ] Sprint 7 — MOPS Alert + PTT Scraper
- [ ] Sprint 7 — Driver PWA (GPS + Photo + Invoice)
- [ ] Sprint 7 — Dispatch Board + Route Optimization
- [ ] Sprint 7 — Flash Sale (LINE OA Broadcast)
- [ ] Sprint 7 — TRCloud Sync

### DocuFlow (Sprint 8) — ✅ MVP ครบ (ยังไม่ได้ UAT จริง)
- [x] ~~4 ระดับเอกสาร + 5 บริษัท~~ (foundation migration)
- [x] ~~Tag System~~
- [x] ~~Expiry Dashboard~~ + cron `docuflow-expiry`
- [x] ~~Vehicle + Driver tracking~~
- [x] ~~Renewal Workflow + AI Comparison~~
- [x] ~~Signature Placement (Box drag-drop)~~
- [ ] External Sign (OTP, ไม่ต้อง Account) — ยังไม่ทำ

---

## 🚀 Next 5 Concrete Steps (ลำดับ — refresh 2026-05-11)

1. **Apply FuelOS Sprint 6.0 migration** — `npx prisma db push` + run `20260511000001_fuelos_sprint6_foundation.sql` ใน Supabase
2. **Sprint 6.1 — Price Engine UI/API** — `/fuelos/price-master` + `lib/fuelos/pricing.ts` (pure compute) + audit log บน publish (FUELOS_PLAN.md §3)
3. **Telegram Bot** — install grammy + `/api/telegram/webhook` + Approval inline `[✅][❌][📊]` (block FuelOS MOPS Alert + CashHub approve flow)
4. **LIFF init จริง** — เปลี่ยน `/liff/report/[branchId]` จาก `requireSession()` → `liff.init()` + map LINE userId → User
5. **CashHub Anti-Stupidity ที่เหลือ** — Time Alert (00:00–05:00) + Pre-check Rule 7

---

## ⚠️ Open questions / Risks

- **Next.js 16 docs**: หลาย API เปลี่ยน, agent ต้องอ่าน `node_modules/next/dist/docs/` ก่อน (ดู `AGENTS.md`)
- **LIFF page ปัจจุบันใช้ `requireSession()`** = ยังไม่ใช่ LIFF จริง (ต้อง login ก่อน). ถ้าจะให้ Staff เปิดจาก LINE Rich Menu ตรง ๆ ต้องเพิ่ม `liff.init()` + map LINE userId → User
- **PROJECT_TRACKER.md ในสเปคล้าสมัย** — ต่อไปอัพเดตที่ STATUS.md นี้แทน
- **Brand wording**: "Pooilgroup" (คำเดียว) — ตรวจ UI strings เก่าที่ยังเขียน "Pool Group" อยู่
- **External accounts ที่ต้องตั้งจริง**: LINE Developers Channel, LIFF App, Telegram BotFather, Cloudflare R2 Bucket (น่าจะมีบ้างแล้ว — ตรวจ `.env.local`)

---

## 📂 Source-of-truth Map

```
ดีเทลv1/                        ← Specs (อ่านก่อนเขียน feature)
├── CLAUDE.md                  Master overview
├── CORE_SYSTEM.md             Auth/User/Dashboard
├── CASHHUB.md                 รายงานยอดสาขา
├── FUELOS.md                  ขายน้ำมัน B2B
├── DOCUFLOW.md                เอกสาร + ลายเซ็น
├── RULES.md                   Coding standards (22 rules)
└── PROJECT_TRACKER.md         ⚠️ ล้าสมัย — ใช้ STATUS.md แทน

web/
├── STATUS.md                  ← ไฟล์นี้ (สถานะจริง)
├── CLAUDE.md                  → @AGENTS.md
├── AGENTS.md                  Next.js 16 warning
├── prisma/schema.prisma       8 tables (DONE)
├── constants/business-types.ts  7 form configs (DONE)
├── lib/auth/permissions.ts    Permission Matrix (DONE)
├── lib/cashhub/reconcile.ts   Reconcile logic (DONE)
└── app/(admin)/cashhub/...    Module-based pages (in progress)
```
