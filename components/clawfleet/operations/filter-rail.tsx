// Spec-named alias for OpsFilterRail.
// Spec (claude-design build report) calls for `filter-rail.tsx`;
// existing wired filename is `ops-filter-rail.tsx`. Both resolve.
export { OpsFilterRail as FilterRail } from "./ops-filter-rail";
